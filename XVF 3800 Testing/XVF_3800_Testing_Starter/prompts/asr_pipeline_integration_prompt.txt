I am continuing an XVF3800 XK-VOICE-SQ66 evaluation project. Use the uploaded XVF3800 documentation, the Windows baseline and digital-testing guide, and the existing timestamped test harness. I also have an ASR evaluation pipeline that currently accepts mono audio and evaluates speech-recognition and speaker-embedding models.

Design a detailed integration plan and implementation guide that connects XVF3800 test outputs to the existing ASR evaluation pipeline without breaking its current dataset and prediction contracts. The integration must support raw microphone audio, amplified microphone audio, normal processed auto-selected beam audio, XVF3800 ASR output, individual beam outputs where available, and repeatable packed digital-injection outputs.

Define a canonical manifest that records source recording, XVF3800 firmware image and version, array geometry, output routing, runtime parameters, channel labels, sample rates, gain settings, test condition, DoA and speech-energy telemetry, and synchronization information. Explain how stereo and six-channel recordings should be split into mono files and associated with the correct metadata.

Create a staged implementation plan for: importing existing XVF3800 run folders; resampling and normalization without corrupting comparisons; running identical ASR, speaker-embedding, VAD, and diarization models across each front-end condition; calculating WER, latency, speaker-verification accuracy, embedding similarity, diarization error, clipping, SNR-related measures, and confidence calibration; performing paired statistical comparisons; and producing tables and plots.

Include recommended Python module boundaries, CLI commands, data schemas, error handling, reproducibility controls, regression tests, and safeguards against comparing non-equivalent signals. Explicitly separate tests that are valid with one mono waveform duplicated across four digital microphone inputs from tests that require real or simulated spatial multichannel audio.
