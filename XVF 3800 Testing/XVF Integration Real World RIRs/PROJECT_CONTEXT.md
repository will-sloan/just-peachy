# Current XVF3800 project context

Hardware decision: **6 September 2026, explicitly supplied by the user.** This supersedes earlier Edifier-based planning and any assumption that a reference microphone or speaker correction must precede normal recording. Existing recordings and frozen validation evidence retain their original hardware descriptions.

## Campaign hardware and behavior

- Fixed acoustic source: **KRK GoAux 4**, using wired playback. Bluetooth is excluded.
- Keep speaker volume, LF/HF EQ, ARC state/saved configuration, Windows/interface output level, software playback scalar and physical orientation/facing convention fixed during formal runs. Record controls and photos; moving to another experimental position does not permit retuning the source or rerunning ARC.
- Primary RIR inputs: the default onboard XVF3800 linear array, **MIC0, MIC1, MIC2, MIC3**. User-stated seated order is left to right. Normal capture uses Category 3 at 16 kHz; verify gain 10 and SYS_DELAY −32 before and after each run. Preserve common timing and relative levels.
- Optional reference: **Dayton Audio EMM-6**, connected through an XLR microphone preamp/audio interface. It is reference-only and never required by normal Play & record.
- An optional initial EMM-6 session can characterize/archive the KRK response and establish a reproducible reference playback level. Save raw reference WAVs, serial-specific calibration file, interface/channel/gain, stimulus identity, source controls, microphone geometry, QC and hashes.
- **No speaker-response correction or deconvolution is required now.** Retain reference evidence so later synthetic-versus-real mismatch can motivate a justified source correction. No automatic EQ, correction or normalization is applied by this recorder. A speaker ARC setting, if used, is part of the fixed physical source and is logged separately.

## Software and metadata

Automatic idle USB24 recovery is now enabled in the local server at the user's request. The monitor shares the capture lease, polls at 5 seconds (failure backoff capped at 30 seconds), verifies linear-array identity and campaign gain/delay, restores USB24 after a responsive reconnect, refreshes PortAudio endpoints, and publishes recovery state to the GUI. It does not repair an interrupted take or start playback automatically. Invalid or missing USB replies wait for a physical reconnect rather than issuing blind resets. See `measurement_app/recovery.py`; audit evidence is in `AUTO_USB24_*` experiment folders. Original capture checks and failure classifications are unchanged.

Keep the existing GUI layout and main one-button array workflow. Speaker settings now include ARC state/configuration, interface output level and orientation notes. `setup.speaker` stores these with LF/HF EQ, volume, Windows level and cable/cabinet. `setup.hardware_context` identifies this revision. `setup.calibration` marks the reference optional and source correction unapplied. Normal requests always use `reference: {"enabled": false}`.

Reference work has its own button and `SPEAKER_REF_*` archive. An optional fitted-calibrator helper has `REFERENCE_CAL_*` archives; absolute SPL is not needed for ordinary relative measurements. Record a source-reference archive ID/path in Reference notes if one exists. Leave it blank if the reference workflow is skipped.

The original Word workbook remains unchanged: §7.1 supports Category 3 as the canonical RIR/HIL domain; §§8.4 and 12 describe sweep/RIR handling; §13.1 retains the complete acoustic chain initially, while source correction is a later refinement (§13.6.4).

## Readiness and next action

**Formal-series setup requested by user:** saved profile **KRK formal measurements — -6 dB** (`68aa4fdb1170451093da71bac700485c`) uses the tested fixed settings, amplified-only recording, reference disabled, P1 acoustic acquisition and trial label `KRK_FORMAL_RIR`. Speaker distance/angle start blank. Room/table and recorder-position names retain their latest values for review. Windows Voice Clarity OFF / speaker enhancements OFF are preserved as user-reported notes; EQ/ARC remain unknown rather than invented. The initial P1 pair is now identified by the user as the farthest placement (2.5 m, 0°); P1 R03 is the closest (0.87 m, −45°) and passed capture/marker checks, with about 24.5 dB broadband margin. Only one closest-position take was present at the setup check. This profile does not reclassify or rewrite pilot archives or claim final RIR qualification.

**Latest P1 capture check (6 September):** P1 R01/R02 at −6 dB both passed all capture and acoustic marker gates, with 362 archive hashes verified. Sweep RMS repeat difference was at most 0.06 dB; coarse sweep-band differences were within 0.44 dB. See [P1 pair review](validation/P1_REFERENCE_PAIR_20260906/REVIEW.md). Level-setting and reference-capture checks are complete; proceed to limited nearest/farthest planned-position captures at fixed controls. Full P1 RIR/decay qualification and P2 remain pending; do not equate saved REVIEW status with final RIR acceptance. This update supersedes the instruction below to collect the first P1 pair.

Use [MEASUREMENT_GUI_GUIDE.md](<C:/Users/amiri/OneDrive/Documents/ChatGPT/XVF Measurement/MEASUREMENT_GUI_GUIDE.md>) and the **KRK GoAux 4 — wired campaign setup** profile. The 6 September P0 level pilot now supports **software −6 dB, Windows 70%, KRK maximum, Windows Voice Clarity OFF and speaker enhancements OFF** as the working settings for P1. R13 (−12 dB) and R14 (−6 dB) passed capture/marker checks and showed approximately 5.75 dB broadband change for the requested 6 dB increase. R14 had about 18.3–18.6 dB broadband sweep/background margin and no clipping. See [level-selection review](validation/P0_KRK_LEVEL_REVIEW_20260906/R13_R14_REVIEW.md). Enter the reported physical controls into the saved setup before future takes; original metadata fields were blank and archives were not rewritten. Keep EQ/ARC fixed and document their actual state. Next record two identical P1 front-position sweeps at the selected level, then qualify repeatability and nearest/farthest positions before expanding. The recorder captures and checks the material used to calculate RIRs; it does not yet generate final RIRs automatically.

XVF capture, acoustic marker and coarse level-scaling checks passed in the user's KRK setup pilot. The source model, physical controls and routing remain user declarations, not hardware detection. EMM-6 interface operation, actual ARC state, measured source geometry and full RIR/decay qualification remain unverified. No reference recording is required for the array pilot.

The evaluation board has a documented eight-hour continuous-use audio limit. At the 6 September check, USB metadata responded while the audio engine did not; a documented idle `REBOOT 0` restored normal operation with gain 10, SYS_DELAY −32 and USB24. Schedule sessions around this hardware limit and verify configuration after resets. The recorder does not automatically restart a board during a measurement.
# Background-noise review update — 2026-09-07

User requests retaining background-noisy captures and labelling them for later noise reduction using recorded pre/post excitation audio. Five Loeb Caf takes have additive GUI labels **Background noise present**. Original RETAKE statuses and all original files remain unchanged; 905 manifest files were reverified. See `validation/LAST_FIVE_NOISE_REVIEW_20260907/NOISE_REVIEW.md` and `noise_annotations.json` for run IDs, source hashes, exact per-channel noise-reference sample ranges and limitations. GUI annotations are served from `measurement_app/static/recording_reviews.json`, matched by run ID plus recording timestamp. Do not misclassify the post-sweep room decay as noise, overwrite original audio, assume stationary noise, or promise complete removal of background speech. Timing qualification remains outstanding. No denoising has yet been applied. These annotations cover only the five reviewed recordings, not automatic classification of new takes.
