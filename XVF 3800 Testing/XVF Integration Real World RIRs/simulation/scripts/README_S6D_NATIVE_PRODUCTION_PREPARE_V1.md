# File-only production native preparation

Purpose: prepare exact fresh manifests for the already selected 176 native confirmations, eight accepted Tk v2 probes and two uninterrupted HOST correctness/drain sessions. This helper reads/hashes existing files and writes only small source/JSON metadata. It starts no model, Tk window, device, supervisor or worker job. Historical sources, waveforms, galleries, profiles and adverse receipts remain unchanged.

Inputs: existing reviewed native manifests and literal order, original34 mono WAVs and 1827.426625-second HOST waveform, exact original galleries/profiles, accepted GUIv3/Tk v2/scorer4260 source and review receipts, a host metadata receipt containing available affinity and root-agreed `[12,13,14,15]`, and a fresh output folder directly under `R/application`. `R` is `simulation/reports/S6D/20260913T195357Z`. No waveform is rebuilt. Actual PCM16 frame counts and SHA are verified once per unique source; model assets are retained by their original bindings and are not repeatedly hashed by this preparation.

Outputs: four manifests (88 exact original, 88 exact repaired, eight Tk, two HOST), `NATIVE176_READY_FOR_PROTOCOL.json` with the first actionable176 job order, prospective reference/scorer bindings, a frozen repository-relative scorer/dependency tree, and `PREPARATION_RECEIPT.json`. Native payload paths are fresh on G; creating the plan does not create those payload directories. The host metadata and preparation receipt are evidence of configuration only, not performance qualification. The two HOST files are the same uninterrupted preexisting30.46-minute input in two fresh fixed-config engines, distinct from the two physical900-second captures.

Original-versus-repaired comparisons retain a composite source/policy interpretation. C105 remains boundary-only diagnostic because its pilot p95 goal failed. The 176 headless timings are not actual GUI render times. The eight Tk probes share one native stream across predeclared views and therefore do not replace all per-condition paced coverage. Correctness scoring still requires accepted closed actual outputs, exact source/protocol ownership, whole-source PCM/journal/dispatch/drain validation with helperbb1b, and separate root approval. Missing references or results are never scored as zero. HOST retains109 complete and13 incomplete opportunities; full-session all-speaker WER stays unavailable.

PowerShell (existing Python, fresh output suffix required):

```powershell
$sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B "$sim\scripts\s6d_native_production_prepare_v1.py" --host-receipt "$sim\reports\S6D\20260913T195357Z\application\NATIVE_EXECUTION_HOST_V1.json" --output "$sim\reports\S6D\20260913T195357Z\application\native_execution_preparation_v1"
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6d_native_production_prepare_v1.py" --host-receipt "%SIM%\reports\S6D\20260913T195357Z\application\NATIVE_EXECUTION_HOST_V1.json" --output "%SIM%\reports\S6D\20260913T195357Z\application\native_execution_preparation_v1"
```

Completed suffixes must not be overwritten. New supervised queue preparation follows the independently reviewed additive native wrapper; this helper does not invent approval or protocol identities. The production queue must use root-accepted censusV4, enforce C50/G75 GiB floors and the shared40 GiB cap, retain75-second cooperative stop grace and explicit owner exit, and use the Sep16 19:53:57Z deadline with closeout reserve. Launch commands belong to the eventual exact queue README after wrapper/queue review and root adoption.
