This helper prepares one controlled serial supervisor queue for all892 remaining repaired C065/C088 confirmations. It reads the exact frozen V2 metadata, preserves all892 job IDs/order and all68 conditional native176 credits, and writes fresh proposal files. It never imports model/application/hardware code, opens PCM or active results, starts a child process, or creates execution approval.

Purpose: replace eight manual pilot/continuation queue transitions with one serial queue while retaining original scientific settings and guards. The source audit proved that measured model costs affect speaker/name eligibility; four concurrent native workers cannot replace controlled serial efficacy evidence. Historical parallel proposals remain untouched and unapproved.

Inputs are pinned in the helper: V2 PREPARATION_RESULT SHA fabab6c7971b81e1e6c32f4713a1ad3a78d44a49d59396ec060c457cad8fa3f8 and concurrency constraints SHA a10d348913d502e85faa69fb7213e746e080fe2221c6661810f570a39283af4e. Their bound four manifests, eight queues/proposals, conditional matrix, forecast, shared sources and executable are verified. Binary model/PCM hashes remain literal and must be verified by the unchanged actual execution guards; this preparation does not read them.

The output directory must be a fresh immediate child of R/runner; the payload directory must be a fresh immediate child of G/runner. Existing paths fail without overwrite. Only metadata is created. Four223-job serial manifest views avoid expanding per-cell manifest parsing to892 entries. Native settings/PCM/profile/gallery/model/source fields stay exact; only fresh output/protocol paths, allocation and scientific-role metadata, and consequent hashes change. One queue concatenates V2 worker1..4 order, each original4+219 order, without outcome selection.

PowerShell or Anaconda PowerShell Prompt (metadata preparation only):
```powershell
$Sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
$Out = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\reports\S6D\20260913T195357Z\runner\native_serial892_preparation_v1'
$Payload = 'G:\Just_Peachy_S6D\20260913T195357Z\runner\native_serial892_preparation_v1'
& 'C:\Users\amiri\anaconda3\python.exe' "$Sim\scripts\s6d_native_serial892_prepare_v1.py" --output $Out --payload $Payload
Get-Content -LiteralPath "$Out\PREPARATION_RESULT.json"
Get-FileHash -Algorithm SHA256 -LiteralPath "$Payload\data\QUEUE.json"
```

Anaconda Prompt or CMD:
```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
set "OUT=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\reports\S6D\20260913T195357Z\runner\native_serial892_preparation_v1"
set "PAYLOAD=G:\Just_Peachy_S6D\20260913T195357Z\runner\native_serial892_preparation_v1"
"C:\Users\amiri\anaconda3\python.exe" "%SIM%\scripts\s6d_native_serial892_prepare_v1.py" --output "%OUT%" --payload "%PAYLOAD%"
type "%OUT%\PREPARATION_RESULT.json"
certutil -hashfile "%PAYLOAD%\data\QUEUE.json" SHA256
```

The named v1 directories are an immutable completed preparation once created. For an explicitly requested reproduction, choose unused immediate child names under the same R/runner and G/runner parents; do not delete or overwrite v1. Run the maintained helper at SIM/scripts, because its repository root is anchored to that location. Frozen copies are for inspection.

Outputs on G: data/CONDITIONAL_MATRIX.json, SERIAL_PROFILE_1..4_MANIFEST.json, QUEUE.json, APPROVAL_PROPOSAL.json, ROOT_ADMISSION_PROPOSAL.json. Outputs on R: PREPARATION_RESULT.json, DATA_READBACK_REVIEW_V1.json and FORECAST.json. The readback compares all892 serialized native jobs and protocol jobs to V2, permits only declared metadata changes, verifies complete exact membership/order and unchanged artifact predicates, and confirms that runtime and authorization paths are absent. It executes no scientific tests.

Root admission is separate and pending. Root must retain C065/C088 after controlled native176 analysis, accept the exact68 predeclared credits (including adverse repeats), confirm full executable/source/input/asset binding and healthy closure, and review the literal892 queue. Accepted credit count is zero here. If a credit fails, preserve the adverse attempt and prepare a separately reviewed replacement; do not silently fill or drop it.

During this queue exactly one neural worker is permitted across all studies, with no simultaneous C/core/diagnostic/native neural job. This is a root allocation constraint; the unchanged runner has no cross-queue neural semaphore. The child keeps the original logical affinity [12,13,14,15] and all model pools at one thread. Other host/hardware activity remains a recorded condition; serial does not imply CPU isolation or latency invariance. Controlled native176/Tk/HOST timing remains separate and must be closed or explicitly separated before root admits this queue.

Limits remain cell180s, supervisor360s, stall180s, heartbeat45s, stop grace75s, C>=50GiB, G>=75GiB, shared40GiB new payload, campaign deadline2026-09-16T19:53:57Z and45-minute closeout reserve. Do not lengthen timeouts to conceal failures. A stopped prefix cannot satisfy unchanged full-source predicates.

The source-length-scaled historical forecast is about19.399–20.910hours for40536.33025source seconds, using77–83s prior44.695s cells. It is not a new throughput measurement or guaranteed completion time. Historical output-size scaling is about12.384GiB and is not an upper bound; existing physical/model outputs, metadata and later scoring share the40GiB cap. Root must reconcile current remaining space and time at admission.

No APPROVAL.json, ROOT_ADMISSION.json, runtime session/protocol/state directory or execution command is emitted by this helper. Root alone prepares actual authority and launches the reviewed runner. The approved runner, wrapper, evidence helper and active queues are not modified.

