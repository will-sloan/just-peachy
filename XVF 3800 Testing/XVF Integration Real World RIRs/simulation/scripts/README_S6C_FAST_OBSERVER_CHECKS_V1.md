# Fast observer source checks

Purpose: source-bound comparison of the new canonical and continuous observer wrappers against preserved originals, plus tiny scanner-installation/restoration guards and bounded closed-subtree size comparisons.

The pre-hardlink and pre-completion-timer drafts are preserved under STAGING/observer_fast_v1. The current source pins the separately checked explicit-stat scanner. Its original run bodies differ only by the observer entry decorator and one root-authorized periodic timer assignment measured at scan completion; the actual native worker body is unchanged. Admission of this wrapper is established only by a completed receipt plus independent review, not by this README.

Inputs: preserved original wrappers, the two new observer wrappers and maintained READMEs, exact frozen epoch4 metadata/common source, existing reviewed scanner evidence and two already closed metadata directories. The helper imports no model modules, reads no actual PCM, predictions or native logs, and does not launch a native worker. Its fresh child executes only the observer context with tiny temporary files.

Outputs after admission: immutable `REPORT/observer_fast_v1/SOURCE_CHECKS_V1.json`, with changed-function AST text, exact unchanged functions, inherited 47+72 tiny checks, source hashes, explicit fresh-process checks and closed-subtree metadata totals. A failed pre-publication assertion is not a completed source admission. Never overwrite an earlier result.

The scope includes explicit installation in both canonical coordinator and fresh child via unchanged original worker bodies with entry decorators and one authorized periodic completion timestamp. Scanner restoration is independently tested on normal and exceptional exits. Current resource roots, original native functions and deadlines are checked; no performance claim follows from these tests. Actual current-tree benchmarking is separately owned and source-bound. Optional link behavior unavailable in the old scanner review remains unavailable, not silently passed.

PowerShell:
```powershell
$sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
$analysis = "$sim\staging\s5_text_metrics\analysis_env\Scripts\python.exe"
& $analysis -B "$sim\scripts\test_s6c_fast_observer_v1.py"
```

Anaconda Prompt / CMD:
```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
set "ANALYSIS=%SIM%\staging\s5_text_metrics\analysis_env\Scripts\python.exe"
"%ANALYSIS%" -B "%SIM%\scripts\test_s6c_fast_observer_v1.py"
```

No installation or environment rebuild is required. `--child` is an internal tiny-check mode, not a model/native mode. Real preparation and run commands belong to README_S6C_PACED_EPOCH4_FAST_V1.md and README_S6C_LONG_NATIVE_EPOCH4_FAST_V1.md. Maintain this document with any helper change.

