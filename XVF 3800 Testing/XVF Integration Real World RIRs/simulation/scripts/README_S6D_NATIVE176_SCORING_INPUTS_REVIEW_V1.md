# Narrow independent review of the native176 input builder

Purpose: inspect the exact frozen builder SHA `e379a78c98c937e32549494da52cf1da68278a40ec67294885c1d4fe19299957` with five targeted metadata probes, without repeating the author's24 checks. It verifies that a reused PID does not hide a second still-live historical instance, exact PID/creation dedup retains both roles, all176 frozen prospective/runtime IDs join despite differing original orders, and malformed/partial trailing journal rows remain rejected even after a synthetic completion event.

Inputs: the existing frozen builder and static prospective/queue/source/scorer/evidence/gallery-map bindings. The target module imports only standard-library definitions. This helper calls `static_inputs`, `pure_apis`, `matched_queue_rows`, `prove_absent` with a fake provider, and `Cache.dispatch` with two tiny synthetic G journals. It never calls target `prepare`/`build`, actual psutil process checks, live queue/state reads, native output or PCM reads, scoring, models, a supervisor, device or approval tool.

Outputs: one fresh G directory with two tiny synthetic journals and `RECEIPT.json`. PASS is limited to source/static metadata and these five probes; it is not FINISH176 acceptance or permission to construct actual scoring inputs. Root's separately authorized first closed job topology inspection is recorded in the independent review report, not performed by this helper.

PowerShell / Anaconda PowerShell Prompt:

```powershell
$sim='C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\anaconda3\python.exe' -B "$sim\scripts\s6d_native176_scoring_inputs_review_v1.py" --output 'G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\native176_scoring_inputs_independent_v1'
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_native176_scoring_inputs_review_v1.py" --output "G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\native176_scoring_inputs_independent_v1"
```

Use a fresh suffix for any root-requested repeat; existing review epochs are preserved. The helper is read-only outside its new G fixture directory. Actual FINISH/immutable owner/keep-awake/census closure, all176 exact process-instance absence, complete source/journal/audit validation and root build/scoring admission remain required later.
