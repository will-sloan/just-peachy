# Prepare conditional serial892 for V5/V3 and the80GiB exception

Purpose: migrate the exact unapproved serial892 V1 proposal to a fresh metadata epoch using root-accepted payload runner V5 and native wrapper V3. All892 ordered native scientific jobs, four223-job manifest groups,68 conditional native176 credits, original PCM/frame/profile/gallery/assets/settings bindings, fixed affinity `[12,13,14,15]`, one inner thread, source pacing, serial allocation, timing/STOP waits and full-source/retention predicates remain unchanged. No parallel queue is produced.

Inputs: pinned V1 `PREPARATION_RESULT.json` SHA `60c14d0a…`; its bound four manifests, queue/proposals, matrix, forecasts and concurrency constraint; accepted V5/V3 source receipt SHA `585c3db4…`; exact exception SHA `079900e5…` and contract/forecast/prior-runner authorities. The helper imports only the hash-pinned, metadata-only V1 builder's read/bind/write/compare utilities. It opens small code/configuration/metadata/executable files to verify bindings. It does not open source PCM, models, active journals/results, or any hardware surface, and does not import native, GUI, scoring or supervisor modules.

Outputs: a fresh R/runner report directory with result/readback/forecast metadata, and a fresh G/runner directory containing four manifests, `CONDITIONAL_MATRIX.json`, `QUEUE.json`, `APPROVAL_PROPOSAL.json` and `ROOT_ADMISSION_PROPOSAL.json`. Large metadata stays on G and is bounded below64MiB; source/report receipts stay small on C. The helper requires C>=50GiB/G>=75GiB during preparation. Native-output, protocol and state roots remain absent. `APPROVAL.json` and `ROOT_ADMISSION.json` are never created, approved-job hashes remain empty, and accepted credits remain0.

The only executable source changes are the already accepted V5/V3 source bindings. Each native manifest declares `limits.max_new_payload_gib=80`, the exact exception and new protocol sources. Every queued job guards the exact exception, original contract, whole-study forecast and prior V4 runner along with the new runtime and inherited application/profile graph. All three shared payload census roots and the original disk floors/deadline/reserve remain unchanged. Historical metadata references remain provenance; no former job output or cache is reused.

After serialization, an independent field comparison within the helper rereads all four manifests and the literal queue/matrix. It permits only declared budget/protocol/output changes, verifies every scientific job field except its output path, all ordered matrix rows/credits, exact executable/helper arguments, unchanged waits/STOP and every artifact predicate apart from rebound hashes/paths. It also checks unique fresh paths, all892 authority guard sets, unchanged source-size limits, and absence of actual approval/output files. This is metadata evidence, not a scientific run, candidate acceptance or runner admission.

PowerShell / Anaconda PowerShell Prompt, preparation only:

```powershell
$sim='C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\anaconda3\python.exe' -B "$sim\scripts\s6d_native_serial892_prepare_v2.py" --output "$sim\reports\S6D\20260913T195357Z\runner\native_serial892_preparation_v2" --payload 'G:\Just_Peachy_S6D\20260913T195357Z\runner\native_serial892_preparation_v2'
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_native_serial892_prepare_v2.py" --output "%SIM%\reports\S6D\20260913T195357Z\runner\native_serial892_preparation_v2" --payload "G:\Just_Peachy_S6D\20260913T195357Z\runner\native_serial892_preparation_v2"
```

Paths must be fresh immediate children of the fixed R/runner and G/runner directories. Preserve prior attempts; do not delete or overwrite an existing preparation to rerun. After this preparation succeeds, rerunning the same command deliberately refuses the existing epoch. No models, launches or actual approvals occur.

Remaining root admission: retain the exact GUIv3 candidate after native176 analysis; independently accept all68 exact full-source credits or explicitly revise the declared remaining population; finish and review native176/C12/Tk/HOST2 controlled serial allocations before892; review the literal new source/manifest/queue graph and current resource/deadline feasibility; then create separate actual approval/admission documents and validate through the exact accepted V5 supervisor. No other neural session may overlap the892 efficacy queue. The historical19.399–20.910h estimate and40536.33025 source seconds are preserved arithmetic, not new timing measurements. September16 19:53:57Z hard end, at least45minutes closeout,480 physical attempts and21600 charged seconds remain fixed. Existing native176/V4 and physical-bank40GiB policies are not altered by this preparation.
