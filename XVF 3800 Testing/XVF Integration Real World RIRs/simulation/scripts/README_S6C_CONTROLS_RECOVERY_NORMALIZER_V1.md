# Recovered controls runtime metadata join

Purpose: use the unchanged reviewed runtime metadata normalizer for both separately completed historical post-analysis generations of the one controls_fast_v1 recovery. No cells are rerun, no metric is rescored, and no failed coordinator status is rewritten.

Inputs: exact external recovery RESULT path/SHA; an explicit finite normalization spec containing the ordinary schema s6c-runtime-normalization-inputs.v1, status EXPLICIT_FINITE_SCOPE, original working_registration binding, exact observer_index, and exactly two requests (historical baseline/B00 and historical research/B01). Each request has its own input_id, original controls manifest binding, generation and completed original historical analysis_receipt. Add external_recovery with the exact path/bytes/SHA binding matching the CLI. physical_inventories must be absent/empty. Sources are the pinned original normalizer, reviewed recovery analysis adapter and recovery inventory overlay.

The new source only supplies a private module/source context to the identical original normalizer run code. All original condition, tail, owner, session uniqueness and analysis-chain functions remain original objects. The recovery inventory context independently validates original80 cells and the exact C archive; failed observer/error and original exit1 remain in proof chains. COMPLETE runtime rows refer to admitted completed native cells and full-source metadata, not a successful original coordinator cleanup. The bound external_recovery and context source list explain the separately authorized closure. Unavailable research instrumentation for original B00 remains unavailable.

Outputs: original RESULT.json, RUNTIME_ROWS.json and PHYSICAL_ROWS.json under a fresh REPORT/runtime_normalization/NAMESPACE. Both40-cell generations are retained separately; physical rows are empty in this bounded join. Final selection, whole-study inventory and scientific score tables are outside scope. The existing normalizer requires actual completed post-analysis for both generations, refuses a current shared lease and preserves existing outputs.

PowerShell (replace explicit bindings; only after independent review and both analysis results close):

```powershell
Set-Location 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts'
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B s6c_controls_recovery_normalizer_v1.py run --recovery 'ABSOLUTE_RECOVERY_RESULT.json' RECOVERY_SHA256 --spec 'ABSOLUTE_FINITE_NORMALIZATION_SPEC.json' --spec-sha256 SPEC_SHA256 --namespace controls80_recovered_metadata_NEW
```

Anaconda Prompt / CMD (existing interpreter; no installation or activation needed):

```bat
cd /d "C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B s6c_controls_recovery_normalizer_v1.py run --recovery "ABSOLUTE_RECOVERY_RESULT.json" RECOVERY_SHA256 --spec "ABSOLUTE_FINITE_NORMALIZATION_SPEC.json" --spec-sha256 SPEC_SHA256 --namespace controls80_recovered_metadata_NEW
```

No actual spec or output is created by these example commands. Source checks are maintained with test_s6c_controls_recovery_normalizer_v1.py and use private synthetic context only, reproducing23 recovery-analysis checks and17 independent private-normalizer checks. The receipt also binds that bounded recovery-analysis source review. Its --output option writes a fresh source-check receipt; replace the helper command above with `-B test_s6c_controls_recovery_normalizer_v1.py --output ABSOLUTE_FRESH_CHECK_DIRECTORY` to reproduce them.
