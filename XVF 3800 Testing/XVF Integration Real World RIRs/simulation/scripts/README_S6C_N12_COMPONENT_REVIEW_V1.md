# Independent N12 compact review V1

`s6c_n12_component_review_v1.py` independently reconciles the completed full C076 N12 scalar tables and interpretation evidence. It is a small, source-pinned adapter of `s6c_n08_n10_component_review_v1.py`, SHA256 `8998e144925df82119f893bfa269f540701141c768c782ddb17701e97924caec`. Twenty-one explicit text substitutions, each with its original occurrence count, adapt only the receipt pins/labels,480-cell scope, eleven contrasts/154 metric rows, C067's additional original source authority,16 C076-versus-C065 extrema output provenance and the owner's largest/smallest signed-difference labels. The original helper is unchanged. The original independent arithmetic is preserved and remains separate from the result owner's summarizer.

Inputs are exact completed N12 core, comparison and summary receipts: core `d980fa246ab444810219961188c0c95ecf71c696d6d7103cf2ac87938b6691db`, comparison `8b3ce24dbda109761bf36bee867e404083b88b3a104786c5b2632ef8d3459563`, summary `154ae3133be46727ad9a7adeca46a366776140c781b309651aa363f4879ad9fe`. The summary binds C065's completed full anonymous authority and C067's exact full N03 authority. Original SCENE, PROFILE, SHORT, TURN, LIFECYCLE and RECIPE_COST CSV buffers are checked against these bindings, plus the completed comparison's selected scene rows/paired rows and uncertainty JSON. The original historical53MB table is not reread; its selected rows remain under the completed comparison authority.

The helper verifies full240 exact routes,203 complete scenes/6016 words/693 turns/292 returns, separate incomplete26/84/32 and empty11 populations, nonuniform actual input durations, support partitions, short observed/missing waits and quantiles, lexical/cp counts, paired room totals/signs, source-cost counts/missing sections, scored text changes and all16 predeclared signed extremes. C067 versus C076 is a whole-condition tradeoff, not an isolated endpoint test. No original scores, vectors, predictions, native logs or audio are recomputed/read. No NN, policy replay, scorer or bootstrap runs.

The output is an exclusive new `REVIEW_RECEIPT.json`, with check count, exact sources/adapter-origin transformations, population and metric witnesses, selected scored-final-text/turn examples and limitations. It is a numerical review, not final selection or native/pace execution. Prose review is recorded separately after this table pass. Zero-delta extremes are explicitly labeled equal; mixed complete word counts combine primary serialized and overlap MIMO scopes. Short waits and nested compute sums do not become paced latency or whole-pipeline wall time.

## PowerShell

```powershell
$s6cRepo = 'C:\Users\amiri\Documents\GitHub\just-peachy'
$s6cSim = Join-Path $s6cRepo 'XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& (Join-Path $s6cRepo '.edge-speech-env\python.exe') -B (Join-Path $s6cSim 'scripts\s6c_n12_component_review_v1.py') --output (Join-Path $s6cSim 'reports\S6C\20260910T123540Z\independent_review\full_n12_component_v1\REVIEW_RECEIPT.json')
```

## Anaconda Prompt or Windows CMD

```bat
set "S6C_REPO=C:\Users\amiri\Documents\GitHub\just-peachy"
set "S6C_SIM=%S6C_REPO%\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"%S6C_REPO%\.edge-speech-env\python.exe" -B "%S6C_SIM%\scripts\s6c_n12_component_review_v1.py" --output "%S6C_SIM%\reports\S6C\20260910T123540Z\independent_review\full_n12_component_v1\REVIEW_RECEIPT.json"
```

The explicit existing EDGE interpreter supplies all standard-library dependencies; no install or `conda activate` is needed. Run only outside an active paced/continuous quiet lease. The output must not exist. If reproduction is explicitly requested after completion, use a new review namespace; do not overwrite the preserved result. Original receipt pins, source bytes and all twenty-one substitution counts are checked before use. No synthetic fixtures or source/scorer changes are introduced by this narrow actual-data adapter.

The first actual review stopped at the extreme-label lookup after earlier table checks because the new owner uses signed-difference labels. Exact prior adapter/README bytes are preserved under `staging/s6c/20260910T123540Z/n12_component_review/pre_label_repair_v1`. The two added translations change labels only; integer deltas and deterministic case ordering are unchanged. No native/scorer failure occurred, and no PASS receipt was published for that attempt.
