# S6B measured pilot admission

Purpose: summarize all64 completed balanced native pilot outputs and record prospective cost estimates before admitting the18 exact neural recipes on the44-scene challenge. This performs no inference, hardware action or selection based on accuracy.

Inputs: the frozen epoch2 execution manifest, pilot receipt index and bound native evidence, input duration index, challenge panel and current resource heartbeat. Outputs: `reports/S6B/20260909T230840Z/PILOT_ADMISSION_AND_ETA.json`. Existing immutable native files are only read. The ETA factors are engineering scenarios; they are not confidence intervals or a CM5 prediction.

Run after the pilot finishes. In PowerShell:

```powershell
Set-Location 'C:\Users\amiri\Documents\GitHub\just-peachy'
& '.\.edge-speech-env\python.exe' '.\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6b_campaign_audit.py' --epoch epoch2
```

In Command Prompt or Anaconda Prompt (uses the same repository interpreter):

```bat
cd /d "C:\Users\amiri\Documents\GitHub\just-peachy"
".edge-speech-env\python.exe" "XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6b_campaign_audit.py" --epoch epoch2
```

The command requires a completed64-output pilot and checks its balanced tap counts. It writes an analysis receipt and never starts the next phase automatically. Research execution, reserves and STOP_REQUEST are documented in `README_S6B_EXECUTION.md`.

After the existing B00 predictions have been scored into `baseline_all_validation_v1`, append `--mode baseline` to either shell command. This checks28 explicit accepted S6A word/cp/population/source-turn values across480 saved outputs and writes `BASELINE_PARITY_RECEIPT.json`. It performs no neural inference. The historical all240 baseline remains separate from new scheduler controls.
