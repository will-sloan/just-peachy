# Sentinel and cross fast_v2 source review

Purpose: verify two new root-authorized derivatives against preserved fast_v1 and final L/Cfast_v2. No original scanner, runtime, asset or PCM tests are repeated. This script never calls prepare, source-checks, run, worker or load_native.

Inputs: held old/new wrapper bytes, final L/Cguard source pins, original D cleanup helper and small previous review receipts. Imports are wrapper/metadata code only. Exact ASTs cover the sentinel native/worker/grid functions and the cross private compiled guarded functions. Synthetic grids preserve12 sentinel and40 asymmetric cross cells. Missing/tampered sentinel guard policy is rejected, and old/v1/v2 quiet recognition is checked.

Outputs: fresh REPORT/observer_fast_v2/SENTINEL_CROSS_GUARD_CHECKS_V1.json with source hashes, focused checks, prior-review links, the guard policy and explicit deferred preparation. Prior files remain unchanged. Refuses an existing output.

PowerShell:
```powershell
& 'C:/Users/amiri/Documents/GitHub/just-peachy/.edge-speech-env/python.exe' -B 'C:/Users/amiri/Documents/GitHub/just-peachy/XVF 3800 Testing/XVF Integration Real World RIRs/simulation/scripts/test_s6c_sentinel_cross_guard_v2.py'
```

Anaconda Prompt or CMD:
```bat
"C:/Users/amiri/Documents/GitHub/just-peachy/.edge-speech-env/python.exe" -B "C:/Users/amiri/Documents/GitHub/just-peachy/XVF 3800 Testing/XVF Integration Real World RIRs/simulation/scripts/test_s6c_sentinel_cross_guard_v2.py"
```

This is source readiness only. Real preparation rehashes model assets and verifies canonical PCM through unchanged original admission, so wait for root to release any quiet paced interval and for independent source acceptance. No native retry or quiet authorization is created.

The initial review omitted module import aliases from its version-only AST normalizer and stopped before publication. Its source/README are preserved under staging/observer_fast_v2/sentinel_cross_before_import_normalization; only that reviewer normalization was corrected. Production wrapper bytes were unchanged.
