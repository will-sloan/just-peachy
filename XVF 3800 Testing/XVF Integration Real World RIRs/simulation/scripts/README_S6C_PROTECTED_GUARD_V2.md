# Focused structural protection guard tests

Purpose: test new L/C fast_v2 integrity guards against the demonstrated marshal reference-state failure while proving the native/coordinator timing body, scanner and prior files unchanged. Only wrapper imports (stdlib/psutil), exact whole-module compilation without executing the frozen module, and pure helper calls are used. There are no model, APP, audio, event, resource-scan or preparation operations.

Inputs: exact held fast_v1 and proposed fast_v2 wrappers/READMEs, pinned original long helper and the model-free diagnosis receipt. No configuration or scientific setting is changed. The source test uses the actual source_bindings function code with a path-only binder stub and original pathlib operations.

Outputs: fresh REPORT/observer_fast_v2/GUARD_CHECKS_V2.json with exact source SHA/length, structural guard policy, old-versus-new warm-call evidence, focused replacement negatives and function AST delta list. V1 source/receipt bytes are preserved in staging/observer_fast_v2/before_quiet_union_v1 with an exact index. V2 explicitly tests the additive original/fast_v1/fast_v2 quiet-recognition union. Existing output is refused. This is source readiness, not native execution admission; parent/source reviewer acceptance is needed before the already-authorized metadata preparations.

PowerShell:
```powershell
& 'C:/Users/amiri/Documents/GitHub/just-peachy/.edge-speech-env/python.exe' -B 'C:/Users/amiri/Documents/GitHub/just-peachy/XVF 3800 Testing/XVF Integration Real World RIRs/simulation/scripts/test_s6c_protected_guard_v2.py'
```

Anaconda Prompt or CMD:
```bat
"C:/Users/amiri/Documents/GitHub/just-peachy/.edge-speech-env/python.exe" -B "C:/Users/amiri/Documents/GitHub/just-peachy/XVF 3800 Testing/XVF Integration Real World RIRs/simulation/scripts/test_s6c_protected_guard_v2.py"
```

Scope: stable typed recursive structural hashes plus runtime function/code IDs; retained original objects prevent same-process ID reuse in protected_override. The manifest's explicit policy names the algorithm and pinned original function hashes; raw marshal digests are not reused as authority. Focused mutations reject new same-value code objects, new functions, altered constants/code/metadata, foreign module/global source and missing/changed guard policy. No old or failed outcome is relabeled.
