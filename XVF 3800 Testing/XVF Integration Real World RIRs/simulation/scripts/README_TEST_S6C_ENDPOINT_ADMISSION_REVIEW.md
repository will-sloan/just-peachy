# Independent endpoint-factorial admission review

Purpose: inspect the exact held C195/C196 registration, epoch6 snapshot,224 prepared native jobs and separate V5 coordinator before execution. This new test preserves the prior cadence reviewer and all owner artifacts. It checks the old384 effective rows unchanged, four real profiles differing from parents only by ID and XVF routing, independent tracking flags, full-profile-and-cue native identity, same once-gained audio bindings, all56 cases including11 source-empty cases, unique keys and fresh endpoint output paths.

Inputs are explicit pinned V6/V7 registry, endpoint amendment, functional/dependency receipts, epoch6, job manifest and coordinator admission/fixture receipt. The test rehashes small Python source modules and admission JSON; model/audio byte admission remains the responsibility of the reviewed original native runner. It verifies whole APP/native/common identity against epochs2/5, reproduces actual frozen Advisor and ASR-loop fixtures with synthetic observations/tiny waveform/stub decoder, checks unchanged V4→V5 coordinator core functions, and checks the actual matrix full-profile digest boundary. It never calls a model or real native run and does not establish quiet/resource availability.

Output: fresh REPORT/independent_review/ENDPOINT_FACTORIAL_ADMISSION_REVIEW_V1.json containing source bindings, check counts and limited pre-execution PASS. Existing receipt is never overwritten. Tests cannot establish actual telemetry activation or improved words. Native/advice coincidence is one reset and not a separate incremental advice success.

## PowerShell

The coordinator run-function comparison permits exactly one version-name change in its rejection message: Prepared V3 admission required becomes Prepared V5 admission required. All other run AST nodes and the seven other coordinator functions must remain exact. The initial reviewer assertion caught this message-only difference; the reviewer now declares it explicitly. No owner code or native behavior was changed.

~~~powershell
$repo = 'C:\Users\amiri\Documents\GitHub\just-peachy'
$sim = "$repo\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
& "$repo\.edge-speech-env\python.exe" "$sim\scripts\test_s6c_endpoint_admission_review.py"
~~~

## Anaconda Prompt / Command Prompt

Use the existing native environment interpreter for exact frozen APP synthetic arithmetic. No installs or activation needed.

~~~bat
set "REPO=C:\Users\amiri\Documents\GitHub\just-peachy"
set "SIM=%REPO%\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"%REPO%\.edge-speech-env\python.exe" "%SIM%\scripts\test_s6c_endpoint_admission_review.py"
~~~

The four profiles complete the none/tracking-only/endpoint-only/both factorial with C065/C079. The inherited N01 recipe label is insufficient for ASR source reuse; actual full profile and cue source are bound. All numeric ASR rules and weights remain the parents' values. This is an outcome-informed missing-interaction amendment, not an untouched preregistration.
