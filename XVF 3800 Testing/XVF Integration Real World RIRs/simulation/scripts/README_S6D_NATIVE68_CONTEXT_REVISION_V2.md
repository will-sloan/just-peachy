# Context-only native68 proposal revision and root acceptance procedure

Purpose: preserve the immutable1c42767f complete68 proposal and create a fresh version changing **only** `current_context`. Root now reports C12 FAILED0/12 because the first `source_started` lacked `pipeline_sample_rate`; the strict bb1b validator refused it. The producer repair is separate. The old C12 failure and sources remain preserved. Scoring ended25.6s before C12 began. These are root-reported historical facts, not refreshed process observations.

Inputs: the one pinned original proposal JSON and this helper/README. Output: one fresh ROOT_RETENTION_AND_68_CREDITS_PROPOSAL.json, still unapproved with accepted_credits=0 and all68 ineffective decisions. An exact JSON comparison ensures every field except current_context is unchanged. No source/model/audio/vector/journal scan, scorer/inference, live process query or approval occurs. Original completeness/source/profile/gallery/vector/settings/context/PCM/pacing proofs and bindings are reused unchanged.

PowerShell:

```powershell
& 'C:\Users\amiri\anaconda3\python.exe' 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6d_native68_context_revision_v2.py' --output 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\reports\S6D\20260913T195357Z\application\native68_root_receipt_preparation_v2\ROOT_RETENTION_AND_68_CREDITS_PROPOSAL.json'
```

Anaconda Prompt or Command Prompt:

```bat
"C:\Users\amiri\anaconda3\python.exe" "C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6d_native68_context_revision_v2.py" --output "C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\reports\S6D\20260913T195357Z\application\native68_root_receipt_preparation_v2\ROOT_RETENTION_AND_68_CREDITS_PROPOSAL.json"
```

## Exact root-only acceptance procedure

This helper has no acceptance mode. The following defines the concrete receipt root can issue after its own hash/content review; it is not a claim that the future runner already has a parser for this new receipt schema.

1. Root verifies the fresh v2 proposal hash, prior1c42767f binding and exact non-context equality. Existing885634 full176 validation,3069 index, analysis, fixed68cc57 and independent422a membership/template review remain bound. Root need not repeat source/audio/model scans without a new concern.
2. Create a new actual acceptance file at a fresh root-owned path; preserve both proposal files. Keep schema `s6d-native176-retention-and-native-cache-credit.v1`, run ID, root owner thread ID, all scientific/input/equivalence/closure/coverage/adverse/preservation fields, all68 IDs and all repeat bindings unchanged.
3. Set `status` to `ROOT_ACCEPTED_NATIVE176_RESEARCH_RETENTION_AND_EXACT68_CACHE_CREDITS`, `root_approval_created=true`, `accepted_credits=68`, and keep `proposed_accepted_credits=68` as provenance. Set `root_acceptance` to an object containing `owner_thread_id`, actual `approved_at_utc`, exact `reviewed_proposal` path/bytes/SHA, and scope `research retention plus exact native prediction cache credit only`. Do not put the new file's own hash inside itself; record its resulting binding externally after creation.
4. Set `retention.status=ROOT_ACCEPTED_RESEARCH_RETENTION_FOR_FULL240_CONFIRMATION_ONLY`. Preserve the three existing candidate proposals/rationales. Add `decision_effective=true` and a `decision` field to each candidate: C065 `RETAIN_ANONYMOUS_RESEARCH_CONTROL`, C088 `RETAIN_LIMITED_NAMED_RESEARCH_CANDIDATE`, C105 `RETAIN_DIAGNOSTIC_ONLY`. `default_promotion` stays false.
5. For each of exactly68 entries, set `decision_effective=true` and add `decision=ACCEPT_EXACT_PREFERRED_NATIVE_PREDICTION_CREDIT`; preserve `proposed_decision`, all evidence bindings, fixedr1 selection and every repeat. No partial approval or favorable r2 substitution is represented by this exact68 receipt. If root rejects a credit, prepare an explicit new scope rather than marking this exact receipt accepted.
6. Keep `future_execution_not_admitted` and zero queue/NN/hardware launch fields unchanged. Issuing these credits alone does not launch892 or satisfy failed C12, unrun Tk/HOST, resource/lease/census/authority/deadline gates. The existing conditional matrix remains an immutable declaration with accepted_credits=0; the actual receipt overlays that status in a separately reviewed root admission.
7. A future892 admission must explicitly bind the new actual receipt and require its exact accepted status/root identity/count/68 entries and the already reviewed960-cell disjoint coverage. Add its binding to each necessary job source guard in a reviewed additive queue/admission step; recompute queue/job approval hashes there. Do not mutate the frozen proposal queue, claim this document itself is a parser or silently treat free-text prerequisites as enforced.

The builder's current scope is context revision only; none of steps2–7 is executed here. Any future implementation must retain one serial native efficacy worker and all original scientific, full-input, resource and campaign limits. No root acceptance is implied by the schema names above.
