# B36 paced fast V2: explicit same-volume lease archival

Purpose: run the exact existing B3640-cell paced experiment with a separately registered coordinator whose final quiet-lease archive stays on C:. Original b36_fast_v1 preparation and all held sources stay unchanged. This source/preparation performs no native work unless root later supplies an exact separate quiet admission.

## Inputs and preserved execution

The sole preparation parent is G:\Just_Peachy_S6C\20260910T123540Z\paced_controls\b36_fast_v1\MANIFEST.json SHA3f1ebec0b5af205b2d215988c692fd510171b52d0114c48110e2142a2563038c. It must remain unstarted. The helper uses the held shared observer SHA579963cb… in a private namespace; original globals and Path.rename are never patched. New schema s6c-historical-paced-b36-fast.v2 explicitly binds this coordinator, its README, previous fast preparation and lease_archival_policy. Namespace must end _fast_v2.

All40 native job objects and keys are exact parent objects: B36 only, original16 cases once plus original4 cases repeated, both O0/O1. Frozen S6B epoch2 B36 profile, native s6b_paced.worker, audio paths/gain/input hashes, decoder/tracker settings, timing/drain bounds, resource limits and scanner remain unchanged. Actual run still performs the original full dependencies/PCM admission. Metadata prepare reads only source and manifest/registry metadata; it does not hash PCM/models, call load_native, infer or launch.

The shared source adapter retains its existing timer rule (20 seconds from scan completion). Relative to its reviewed runtime body, the sole run AST change replaces final lock.rename with archive_finished_lease. Schema/source/namespace projection changes are separately explicit; native function bodies are unchanged. Original interpreter, quiet lease, one child at a time and all native observation/failure checks remain. Original deadline is not extended.

## Closure records

Original jobs and invocation LAUNCH/COMPLETION stay under PAYLOAD/paced_controls/NAMESPACE on G:. After successful40/40 original completion, the new final call checks exact manifest/owner/argv/lease and every original child COMPLETE identity and current closure. It renames the exact lease bytes to REPORT/paced_controls/NAMESPACE/invocations/ORIGINAL_INVOCATION_ID/PRESERVED_ORIGINAL_QUIET_LEASE.json on C:, using the exact reviewed canonical release_lease function extracted through the held recovery utility. The recovery utility's event-specific recover command is never called.

A durable sibling LEASE_RELEASE.json has schema s6c-b36-paced-lease-release.v1 and binds new coordinator/manifest, original COMPLETION, exact source lease/archive, current child observations, source policy and all40 completion references. Original G:QUIET_OWNER_CLOSED.json is intentionally absent. No cross-volume copy, delete or fabricated old success record occurs.

A partial native batch, live/unknown child, changed lease, archive collision or IO failure retains or reports unverified release and fails the original coordinator. The failed receipt and original evidence remain; no automatic retry exists. If receipt publication fails after rename, root must inspect the C archive and original evidence rather than resume. The coordinator itself is still exiting when it records release; downstream admission must freshly observe its exact PID/creation closed.

The unchanged shared outer observer emits SCANNER_OUTCOME.json under REPORT/paced_controls/NAMESPACE/observer_invocations with actual new coordinator owner/source identity. Closed-batch admission requires its exact historical schema, integer coordinator PID and all three REPORT/STAGING/PAYLOAD scan counter records: nonnegative integer counts, calls equal successful plus failed, at least one successful scan per root, nonnegative last bytes, and finite nonnegative scan clocks. These are the held V7 counter relationships, reused locally without importing inventory. Original source statuses are never projected to successful closure.

## Metadata APIs and downstream admission

context() returns an isolated explicitly projected shared adapter. admit_plan(reader,binding) returns exact plan,binding, checking the new metadata projection. reader.read(path,expected=None) returns parsed JSON plus exact path/bytes/sha256 binding. Runtime original full admission remains separate.

admit_complete_batch(reader,binding,inspect=exact_process_state) checks the global active lease absent, one original invocation, original40 completion count/grid, original per-cell owners freshly closed, successful new C lease-release receipt/archive byte chain, one exact successful observer exit with unchanged sources, and closed coordinator command identity. It returns B36_NATIVE_BATCH_COMPLETE_WITH_EXPLICIT_C_ARCHIVE and all compact proof bindings. It does not reopen native payloads or calculate accuracy.

Old V7/dispatcher/post-analysis must reject this new schema until a separately reviewed integration explicitly calls these APIs or reproduces their guards. No helper/manifest alias or silent old-G archive assumption is permitted. Original complete-native physical counts, observer status and scientific analysis remain distinct.

## Outputs and commands

prepare writes a fresh PAYLOAD/paced_controls/NAMESPACE/MANIFEST.json and REPORT/paced_controls/NAMESPACE/PREPARATION.json. Run is separately authorized. Tests write only a bound SOURCE_CHECKS.json and private fixtures; test_s6c_paced_b36_fast_v2.py shares this README. Source/grid/AST/closure tests do not launch native work.

PowerShell:

```powershell
Set-Location 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts'
$edgePy = 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe'
& $edgePy -B test_s6c_paced_b36_fast_v2.py --output '..\reports\S6C\20260910T123540Z\paced_controls\b36_v2_source_checks_NEW'
# Only after source review; the prior preparation must remain unstarted:
& $edgePy -B s6c_paced_b36_fast_v2.py prepare --source-manifest 'G:\Just_Peachy_S6C\20260910T123540Z\paced_controls\b36_fast_v1\MANIFEST.json' --source-sha256 3f1ebec0b5af205b2d215988c692fd510171b52d0114c48110e2142a2563038c --namespace b36_fast_v2
# Only with exact separately reviewed root quiet authority:
& $edgePy -B s6c_paced_b36_fast_v2.py run --manifest 'G:\Just_Peachy_S6C\20260910T123540Z\paced_controls\b36_fast_v2\MANIFEST.json' --quiet-admission 'ABSOLUTE_ROOT_QUIET_ADMISSION.json'
```

Anaconda Prompt / CMD (existing interpreter; no environment changes):

```bat
cd /d "C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B test_s6c_paced_b36_fast_v2.py --output "..\reports\S6C\20260910T123540Z\paced_controls\b36_v2_source_checks_NEW"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B s6c_paced_b36_fast_v2.py prepare --source-manifest "G:\Just_Peachy_S6C\20260910T123540Z\paced_controls\b36_fast_v1\MANIFEST.json" --source-sha256 3f1ebec0b5af205b2d215988c692fd510171b52d0114c48110e2142a2563038c --namespace b36_fast_v2
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B s6c_paced_b36_fast_v2.py run --manifest "G:\Just_Peachy_S6C\20260910T123540Z\paced_controls\b36_fast_v2\MANIFEST.json" --quiet-admission "ABSOLUTE_ROOT_QUIET_ADMISSION.json"
```

All namespaces above are exact declared preparation examples, not permission to overwrite existing outputs or launch runtime. Keep source checks in a new output directory each time.

Before the first preparation, independent review found missing observer schema/PID/counter guards. The prior source, test, README and 37-check receipt are preserved under STAGING/b36_v2/before_observer_guards_v1/SOURCE_INDEX.json. The repair adds only prospective completed-batch admission and negative tests; the runtime/native/archive call is unchanged. Tests also check exact counter-function AST equivalence after substituting the local root/canonical path expressions.
