# Admit aggregate-only width recovery

Purpose: prepare the literal recovery queue after root review of the finite process-identity repair and the successful actual analysis-venv launcher/worker/supervisor probe. This file-only helper does not launch a scorer or model. It never alters old scores, the old failed queue, or the mathematical analysis functions.

Inputs: exact `runner/width_score_recovery_preparation_v2/RECOVERY_PLAN.json`, source/helper freeze, root's seven independent checks, actual topology probe and its complete supervisor closure,3840 reusable committed scores plus1920 original controls and inherited campaign limits. Outputs: fresh `runner/width_score_recovery_queue_v1` admission, literal queue, approval and preparation receipt. Its child reconstructs required aggregates/uncertainty under a new sibling analysis directory, verifies all immutable inputs again and makes zero new per-cell score calls.

PowerShell:

```powershell
$sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B "$sim\scripts\s6d_recovery_queue_admit_v1.py"
```

Anaconda Prompt / CMD (no activation required):

```bat
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B "C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6d_recovery_queue_admit_v1.py"
```

After root reviews the generated queue, use the exact runner command from the actual queue's README_RUN.md, with `--validate-only` first. Do not rerun an existing queue or substitute an interpreter. The actual scorer child remains in the pinned analysis venv; Windows creates a distinct bound Anaconda worker and both identities are recorded. New heartbeats and STOP requests bind to the supervisor-owned launcher with a proven direct worker relationship. Synthetic STOP validation is distinct from actual normal probe completion. Explicit desktop sleep remains possible despite keep-awake, and the hard campaign deadline is unchanged. Raw inputs/control populations, uncertainty routines and per-case adverse outcomes remain unchanged. Recovery completion is not S6D completion or a deployable profile endorsement.
