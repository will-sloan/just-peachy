# Prospective exact-stat observer version

This separate version preserves the original scientific routes, waveform inputs, model/native functions, score definitions, and output schema/directory family. Every actual namespace must end in _fast_v1; old sources and prepared manifests stay unchanged. Commands below use the new wrapper.

The canonical fast helper installs only the loaded frozen common module's tree_bytes observer. Each nondirectory size uses fresh os.stat, avoiding stale Windows directory-entry sizes after hardlink growth. Traversal/access/unsupported reparse failures are explicit. Periodic full scans start at least 20 seconds after the preceding scan completes; all mandatory full checks, cheap checks, limits, timeout and headroom remain. About 40 seconds per full scan was measured: resource samples are irregular and add host overhead. observer_policy is required in each new manifest. Separate observer_fast_v1/attempts exit receipts record exact wrapper/owner/manifest, scanner timings, errors and restoration; these do not establish native/process closure.

Cross routes retain the original B36 long helper solely for its previously reviewed pure JSON-save and bounded child-cleanup utilities; its run/worker/resource scanner is never executed here. The original immediate spawn receipt and unknown-creation lease retention remain.

Preparation validates inputs and writes JSON metadata only. Run consumes the exact manifest plus an agent-generated quiet admission after all other native/heavy work has closed. Do not infer runtime success from preparation or source checks. See the canonical fast helper README and shared observer README for detailed policy and restoration behavior. The inherited instructions below describe purpose, inputs, outputs, PowerShell and Anaconda/Command Prompt invocations; previously cited original-source receipts remain historical evidence, not new-wrapper execution.

# Exact registered cross-route paced option V1

`s6c_paced_cross_routes_fast_v1.py` provides a separately named, source-only prepared option for **C085 O0-ASR/O1-ID** and **C086 O1-ASR/O0-ID**. It uses the original proposal's 16 whole cases and its four repeated cases: 32 first-pass cells plus 8 repeat cells, 40 total. Each candidate has only its actual registered route. It does not fabricate the other ASR tap for a candidate, substitute an alias or select a finalist. Both routes retain the registered N01, cues-off, no-gallery profile, differing from the corresponding C065 parent only in profile ID and identity audio tap.

## Source reuse and limits

The original canonical adapter SHA `b4b0dc48190654edbdcb6259cf2b8abc08d8675d49367863ff539f7eb54df8a3` remains historical evidence. This version compiles the separately pinned s6c_paced_epoch4_fast_v1.py function ASTs into a private namespace, including its explicit observer scope, policy admission and completion-based timer. Overrides are limited to exact candidate/route/16+4 selection, source bindings, distinct schema/path/lease labels, additive recognition of this coordinator in the existing quiet process check, and the explicit coordinator launch-safety repair below. The receipt records which functions are AST-identical and every controlled change. Original module globals are not patched.

The native function remains exact `s6c_long_session.native`, using the original epoch4 APP, paired input adapter, models, dispatch, state, finalization and process sampler. Actual execution epoch4 is pinned to `720a6cc6c1f9a11ea5d39c3c7f2ab52452aad507ea3d0e0a56fa2c3074af9945`. The native body runs one whole canonical scene per fresh child/model/session. Its inherited generic long-scope text remains in the original RESULT; the new CELL_RESULT and unchanged canonical per-scene source record define the actual input. No concatenation is asserted.

Only JSON metadata is created during a later approved `prepare`. Both existing PCM views and paired telemetry will then be verified at their exact original paths. No waveform copy, trim, new gain, time shift or source-context change is introduced. The once-gained O0/O1 input history remains as admitted. The source-only checks performed before approval do **not** perform this PCM/asset verification.

The existing shared `PACED_QUIET_OWNER.json`, explicit root quiet admission, current worker checks, C:50 GiB/G:75 GiB/RAM12 GiB headroom and 120 GiB new-output cap remain. One child and inner numeric pools1 are retained. The inherited per-cell timeout is `max(720, duration*1.75 + lane_drain_timeout + 120)` seconds, with the registered lane drain unchanged. The run is bounded by the earliest explicit quiet expiry, plan deadline and study deadline, and by the original 8-hour coordinator cap. Missing resources/ownership or failed native completion remain failure, not success.

## Narrow successful-Popen safety repair

Immediately after successful `Popen`, before querying creation time, the coordinator writes `SPAWNED_PROCESS.json` with the owned handle's PID, original child argv, exact manifest/job identity and **null creation time**. An identity-query failure therefore remains an observed physical attempt. Creation time is never inferred and PID-only targets are never signaled.

The parent failure path reuses only the generic, already reviewed `cleanup_attempt` and `final_owner_states` utilities from held `s6c_long_b36_v1.py` SHA `09b809f6301e696054c4563fdad0d48072ae22716bafcf9002a33bdffd866c66`. This does not call its B36 native worker or use its model/source/profile. The shared cleanup deadline is 25 seconds across owned-handle termination/join, verified owned-tree termination/kill and final reap; blocking OS operations cannot be preempted. If creation identity/descendant accounting is unknown, the quiet lease remains retained even when the Popen handle exits. The original native worker is unchanged. If the initial spawn record cannot be written, failure still retains the child handle for cleanup and unknown identity prevents release.

Normal native/cell/launch/completion checks, resource observations, complete-only reuse and measurement recomputation remain the original canonical functions. The parent OUTCOME additionally records `launch_cleanup`; failed/partial/reused states remain distinct. New inventory and post-analysis adapters must explicitly admit these new schemas before treating future outputs as final evidence.

## Inputs and outputs

Inputs: exact epoch4, the held proposal SHA `f303d7e80bc9dd8fa8b7ba7444216d1e6b29f75a0cce8762c40d92ce5201c6da`, original canonical input/profile/source bindings, and later an explicit quiet-admission JSON. Preparation accepts only `C085,C086` in that order and `cross16_plus4` mode. First pass uses C085 then C086 per case; repeat pass reverses that order. Each underlying physical view remains dependent repeated data.

New report/payload roots are `REPORT/paced_cross_routes/<namespace>` and `G:/Just_Peachy_S6C/20260910T123540Z/paced_cross_routes/<namespace>`. The plan schema is `s6c-cross-route-paired-paced.v1`; cell schema is `s6c-cross-route-paced-cell-result.v1`. Outputs follow the original MANIFEST/source JSON, invocation/quiet lease, per-cell launch/spawn/native/result/complete, process trajectories, heartbeat, PACED_INDEX, OUTCOME and CLOSURE layout. Immutable outputs refuse overwrite and partial attempts require a diagnosed new namespace. No cleanup of historical artifacts is provided.

Measured throughput/ETA, model load, source-clock offsets, queue samples, memory and writes inherit the reviewed canonical definitions. They do not guarantee continuous maxima, phonetic/GUI/CM5 latency or absence of a memory leak. Native samples start after model load; the coordinator also samples the child externally. Synchronous load/finalization and observer overhead retain the disclosed original limits. No output from `checks` or `source-checks` proves actual paced completion.

## PowerShell

Only the first source-check command is for the current source-review stage. Actual `prepare` hashes PCM/assets, and `run` executes models; both require the later root task authorization and independent source review. Example hashes/deadlines must be supplied from actual approved metadata, not invented.

```powershell
$repo = 'C:\Users\amiri\Documents\GitHub\just-peachy'
$sim = Join-Path $repo 'XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
$edge = Join-Path $repo '.edge-speech-env\python.exe'
& $edge -B "$sim\scripts\s6c_paced_cross_routes_fast_v1.py" source-checks --output "$sim\reports\S6C\20260910T123540Z\paced_cross_routes\SOURCE_CHECKS_FAST_V1.json"

# Later approved preparation only:
& $edge -B "$sim\scripts\s6c_paced_cross_routes_fast_v1.py" prepare --namespace cross_panel_fast_v1 --candidates C085,C086 --panel-mode cross16_plus4 --deadline-utc 'APPROVED_FUTURE_UTC'
# Later approved quiet interval only:
& $edge -B "$sim\scripts\s6c_paced_cross_routes_fast_v1.py" run --manifest "$sim\reports\S6C\20260910T123540Z\paced_cross_routes\cross_panel_fast_v1\MANIFEST.json" --quiet-admission 'C:\exact\QUIET_ADMISSION.json'
```

## Anaconda Prompt / CMD

No environment installation or activation is needed; the exact EDGE executable selects the current research runtime.

```bat
set "REPO=C:\Users\amiri\Documents\GitHub\just-peachy"
set "SIM=%REPO%\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"%REPO%\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6c_paced_cross_routes_fast_v1.py" source-checks --output "%SIM%\reports\S6C\20260910T123540Z\paced_cross_routes\SOURCE_CHECKS_FAST_V1_CMD.json"
"%REPO%\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6c_paced_cross_routes_fast_v1.py" prepare --namespace cross_panel_cmd_fast_v1 --candidates C085,C086 --panel-mode cross16_plus4 --deadline-utc "APPROVED_FUTURE_UTC"
"%REPO%\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6c_paced_cross_routes_fast_v1.py" run --manifest "%SIM%\reports\S6C\20260910T123540Z\paced_cross_routes\cross_panel_cmd_fast_v1\MANIFEST.json" --quiet-admission "C:\exact\QUIET_ADMISSION.json"
```

`checks` runs tiny synthetic grid/route/closure/deadline fixtures plus the original canonical 72-check group. `source-checks --output FRESH.json` also admits only the pinned epoch/proposal metadata and two registered rows, and binds the source/README/controlled AST changes. Neither starts a native process, reads PCM/model assets, scans a native bank or creates an actual candidate MANIFEST. Every new code version must keep this README and source-only check bindings current.
