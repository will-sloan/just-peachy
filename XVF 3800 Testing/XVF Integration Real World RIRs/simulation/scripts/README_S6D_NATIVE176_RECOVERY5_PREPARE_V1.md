# Native176 five-job recovery preparation

Purpose: `s6d_native176_recovery5_prepare_v1.py` creates metadata for exactly the five declarations remaining after the September14 18:37Z payload-census stop. It preserves the original171 completed checkpoint records and the stopped C105 attempt. It does not run a supervisor, query live processes, perform a new payload census, read source audio/journals/model weights, issue approval or run models/hardware. Only standard-library metadata/source verification and fresh file creation are used.

Inputs are the pinned original QUEUE7cb1efcf, APPROVAL0a61a716, CHECKPOINT4ebc40c4, immutable closure0c567…, lock8ea8001c, stopped launch88862bac, STOPb9b88454 and inner RESULT2d9cfb3f, plus the original manifests and their small source/profile/gallery bindings. The original remaining IDs and order are fixed in source. The original stopped inner RESULT is complete, but its full-source audit and protocol COMPLETION are absent; it receives no credit. Current PID/creation absence remains a root admission prerequisite, not a claim of this metadata tool.

Output `--output` must be a fresh direct child of the run's report/runner directory. The preparation creates two small manifests, an exact5 QUEUE, an empty-approved-list APPROVAL_PROPOSAL, ROOT_ADMISSION_PROPOSAL, RECOVERY_SCOPE, COMPOSITE_MAP, PREPARATION_RESULT and frozen helper/README copies. New runtime paths are declared under `G:\Just_Peachy_S6D\20260913T195357Z\application\native176_recovery5_v1` and `G:\Just_Peachy_S6D\20260913T195357Z\runner\native176_recovery5_v1`; these runtime roots must be absent and are not created by preparation. All historical state and outputs remain untouched.

The original scientific/native job IDs, profile/gallery/audio/PCM/frame/settings/role/repetition fields are unchanged. Each queue execution ID adds `_recover1`, with a distinct native-output and protocol namespace; wrapper874d already distinguishes its environment execution identity from `--native-job-id`. No wrapper or model source changes are needed. Full-source expected artifacts, source pacing, original timeouts/STOP grace, affinity12–15 and one thread per pool remain exact. RunnerV4/874d, all three payload roots,40GiB, C50/G75,60-second complete-census freshness and the original deadline/reserve are unchanged.

Run from the maintained SIM/scripts helper; the copied source_epoch is the immutable review/guard snapshot, not a relocated runnable project. PowerShell / Anaconda PowerShell Prompt:

```powershell
$Sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\anaconda3\python.exe' -B "$Sim\scripts\s6d_native176_recovery5_prepare_v1.py" --output "$Sim\reports\S6D\20260913T195357Z\runner\native176_recovery5_preparation_v1"
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_native176_recovery5_prepare_v1.py" --output "%SIM%\reports\S6D\20260913T195357Z\runner\native176_recovery5_preparation_v1"
```

If that preparation directory already exists, preserve it. A different preparation epoch requires root review because the fixed future runtime roots cannot already be used. There is no automatic retry or overwrite.

`RECOVERY_SCOPE.json` binds the original authorities and exact five scientific/new-execution mappings. New queue jobs guard that scope, exact helper snapshots, old authority documents and unchanged native sources. `COMPOSITE_MAP.json` is created after the queue and binds its literal hash without a circular queue/source hash. Its176 ordered rows select171 `original_completed` records or five `recovery_required` rows. The old queue is explicitly REPORT_BLOCKED; do not manufacture FINISH176 or transplant its checkpoint into a new supervisor state.

The additive closed-input builder must validate each old171 saved completion against its original queue/identity and full actual journal/dispatch/scientific evidence, and require exact new5 approval/FINISH5/closed owner/census/keep-awake/identity/full-source completion. It must verify PID+creation absence for both source populations, keep the stopped attempt excluded and join to the original176 scientific declaration by ID/order. A saved checkpoint alone is not final scientific admission. Root should review this composite contract before scoring.

Before any launch, root independently reviews the proposal, verifies exact old supervisor and stopped launcher/worker PID+creation absence, closes competing allocations and approves fresh paths/current resources/deadline feasibility. The initial five use one serial NN worker with no concurrent physical supervisor or other NN/timing workload. Preserve the historical interruption and changed concurrency condition in timing interpretation. V4 still stops if a census becomes older than60 seconds. Do not extend the bound, reduce accounting scope, change intervals or silently retry.

Root then issues actual `ROOT_ADMISSION.json` and `APPROVAL.json` in the preparation directory, copying only the exact five proposed job hashes and preserving allowlists. Insert their reviewed hashes below. These commands are **future root actions**, not part of preparation.

PowerShell:

```powershell
$R = "$Sim\reports\S6D\20260913T195357Z"
$D = "$R\runner\native176_recovery5_preparation_v1"
$Py = 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe'
$QueueSha = 'ROOT_INSERT_REVIEWED_QUEUE_SHA256'
$ApprovalSha = 'ROOT_INSERT_REVIEWED_APPROVAL_SHA256'
$State = 'G:\Just_Peachy_S6D\20260913T195357Z\runner\native176_recovery5_v1\supervisor_state'
& $Py -B "$R\runner\source_epoch_census_v4\s6d_runner_v1.py" --queue "$D\QUEUE.json" --queue-sha256 $QueueSha --approval "$D\APPROVAL.json" --approval-sha256 $ApprovalSha --state-dir $State --validate-only
```

After root reviews validation and separately admits execution:

```powershell
& $Py -B "$R\runner\source_epoch_census_v4\s6d_runner_v1.py" --queue "$D\QUEUE.json" --queue-sha256 $QueueSha --approval "$D\APPROVAL.json" --approval-sha256 $ApprovalSha --state-dir $State --keep-awake
```

Anaconda Prompt / CMD:

```bat
set "R=%SIM%\reports\S6D\20260913T195357Z"
set "D=%R%\runner\native176_recovery5_preparation_v1"
set "PY=C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe"
set "QUEUE_SHA=ROOT_INSERT_REVIEWED_QUEUE_SHA256"
set "APPROVAL_SHA=ROOT_INSERT_REVIEWED_APPROVAL_SHA256"
set "STATE=G:\Just_Peachy_S6D\20260913T195357Z\runner\native176_recovery5_v1\supervisor_state"
"%PY%" -B "%R%\runner\source_epoch_census_v4\s6d_runner_v1.py" --queue "%D%\QUEUE.json" --queue-sha256 "%QUEUE_SHA%" --approval "%D%\APPROVAL.json" --approval-sha256 "%APPROVAL_SHA%" --state-dir "%STATE%" --validate-only
```

After the separate root launch decision:

```bat
"%PY%" -B "%R%\runner\source_epoch_census_v4\s6d_runner_v1.py" --queue "%D%\QUEUE.json" --queue-sha256 "%QUEUE_SHA%" --approval "%D%\APPROVAL.json" --approval-sha256 "%APPROVAL_SHA%" --state-dir "%STATE%" --keep-awake
```

Root owns process launch/supervision and actual post-run acceptance. No old171 model call is repeated. Actual output/source-binding changes require a fresh declared attempt and independent review; this helper makes no claim that S6D or native176 has completed.
