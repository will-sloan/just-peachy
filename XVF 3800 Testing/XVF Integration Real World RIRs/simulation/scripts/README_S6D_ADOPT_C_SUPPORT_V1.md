# Adopt the reviewed C source-support intervals

`s6d_adopt_C_support_v1.py` issues root's reviewed conservative source-support authority for12 existing C captures. It checks exact preparation/proposal hashes, all underlying metadata bindings, original-to-final payload equivalence, case/partition/A15 joins, source frame bounds and every canonical acceptance digest. It then inserts the actual root binding into new copies and executes the unchanged frozen calibration V2 authority predicate on all12. It performs no model, feature, audio, process, UI or hardware work.

Inputs are the fixed reviewed preparation in `reports/S6D/20260913T195357Z/physical_C_support_final_preparation_v1`, original supports, accepted C partition/catalog, original A15/source metadata and frozen V2 helper. Their hashes are explicit in the script/proposal. The optional `--output` must name a fresh direct child of the campaign report directory; default is `physical_C_support_adopted_v1`. Existing outputs reject and are preserved.

Outputs: actual `ROOT_ACCEPTANCE.json`,12 `*_SUPPORT.json` copies and `SUPPORT_CATALOG.json` with final file bindings and all12 actual V2 predicate results. Only top-level status differs from the original proposals, followed by the root binding insertion. The canonical digest excludes only that binding, avoiding a hash cycle.

PowerShell / Anaconda PowerShell Prompt:

```powershell
$sim='C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B "$sim\scripts\s6d_adopt_C_support_v1.py"
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6d_adopt_C_support_v1.py"
```

This is a historical root-coordinator action after review, not a command to create duplicate authority. Do not rerun it into another directory without a new root reason. If verification fails, preserve the partial outputs and report the failure; do not overwrite them.

The authority preserves138 timing measurements,128 possible interiors and124 exclusions, with15 enrolled/15 nonmember C speakers. These are source-support fragments, not model events. Unknown profiles remain null; ambiguity is not negative truth. Exact per-focus delay, source survival or acoustic assignment is not established. Feature extraction must wait for actual12 native closures; thresholds require separate joint C evidence and exact-context acceptance. No Q fitting, new RIR alignment, waveform changes, model calls or selector enablement occurs here.
