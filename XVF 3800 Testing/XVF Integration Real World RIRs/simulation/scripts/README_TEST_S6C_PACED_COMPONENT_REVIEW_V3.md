# Independent paced V3 component/source review

Purpose: verify the held canonical paced adapter V3 repairs and exact six-case diagnostic selection without launching models. Inputs are the SHA-pinned `SOURCE_CHECKS_V3.json`, held source/README, preserved V2 sources, exact proposal metadata and the already validated32-view source receipt. The review re-executes72 pure temporary fixtures, checks unchanged native-worker/source functions by AST, and adds independent gate/owner/lease/source-duration checks. It does not call `prepare`, `source_checks`, `run`, `worker`, load model assets, re-read real audio or rescan experiment banks.

Output is new immutable `reports/S6C/20260910T123540Z/independent_review/PACED_COMPONENT_REVIEW_V3.json`. It binds the exact original source receipt, current helper/readme and reviewer source. Existing outputs are rejected. A source review is not native launch authorization or completed paced measurement.

PowerShell:

```powershell
$repo = 'C:\Users\amiri\Documents\GitHub\just-peachy'
$sim = Join-Path $repo 'XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
$env:PYTHONDONTWRITEBYTECODE = '1'
& "$repo\.edge-speech-env\python.exe" "$sim\scripts\test_s6c_paced_component_review_v3.py"
```

Anaconda Prompt / Windows CMD:

```bat
set "REPO=C:\Users\amiri\Documents\GitHub\just-peachy"
set "SIM=%REPO%\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
set "PYTHONDONTWRITEBYTECODE=1"
"%REPO%\.edge-speech-env\python.exe" "%SIM%\scripts\test_s6c_paced_component_review_v3.py"
```

The exact existing EDGE interpreter is used; no installation.72 native-free fixtures create and remove isolated synthetic metadata/tiny byte journals through the held helper. Source/root bytes remain unchanged. The full16+4 selection remains40 cells per candidate; gate6 accepts only C071/C082 in fixed order, six proposal cases, both taps, rep1 only:24 cells and1,072.6905 actual source seconds.

Completed reuse must match the entire original cell/native/finalization/artifact/measurement chain. Any alive or uninspectable recorded child retains its quiet lease. Native/outer observers still have sampled, potentially incomplete process trajectories; no continuous resource maximum or CM5 qualification is asserted. Candidate preparation, exact grid review and quiet run admission remain later steps.
