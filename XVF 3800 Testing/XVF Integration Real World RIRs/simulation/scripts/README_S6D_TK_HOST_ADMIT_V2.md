# Tk8/HOST2 admission after whole54 physical-bank completion

Purpose: correct the single F1 prerequisite finding in the preserved V1 admission helper. This additive V2 does not change the existing eight Tk jobs, two HOST jobs, source/runtime controls, actual C12 acceptance, later Tk8 acceptance requirement, CPU12–15, one neural stack, pools1, shared40GiB, C50/G75 floors, deadline or admission write order. It does not launch a supervisor or model. Production uses this maintained scripts path; frozen copies are review evidence.

`--prepare` reads only the exact old queue/proposal/source/manifest metadata and writes four unapproved plans/review proposals to a fresh directory. `--admit` remains a separate root-only operation requiring exact review path/hash, whole predecessor evidence, fresh output paths, current allocation inventory, disk floors and V4 validation before authority writes. This proposal does not execute either entry point. No active physical state is read while building or testing this source.

## Exact future PhysicalBank acceptance

The parent root review retains `prerequisite_acceptances.PhysicalBank={binding,expected_status}`. `expected_status` must equal `ROOT_ACCEPTED_PHYSICAL_BANK_V6_FULL54_AND_OWNER_CLOSURE`. The separately root-issued bank receipt must contain that exact status, owner/run, exact pinned bankV6 queue351a8e52, `accepted=planned=54`, `all_owners_closed=true`, `closed_instances`, and `phase_closure`. These are54 supervisor stages, not433 historical-plus-planned physical attempts. No future acceptance is created by this source proposal.

`phase_closure` contains exact bindings for `queue`, `checkpoint`, `supervisor_closure`, and `closed_owner_lock`, plus `state_dir`. The state directory must be `G:/Just_Peachy_S6D/20260913T195357Z/runner/bank_queue_v6/supervisor_state`, exactly as the immutable bankV6 `ROOT_LAUNCH.json` records (SHA256 `e2efa46bc6f955dc95884d075d5d576c3ee7128621dc9af3e34dea83c88d665b`). The checkpoint must be its `CHECKPOINT.json`, have the exact queue hash, `status=FINISH`, `active=null`, and all54 exact job IDs completed with exit0, `DECLARED_ARTIFACTS_VERIFIED`, and matching finite PID/creation-time/run/job identities. The queue retains all54 original ordered hardware job IDs; a replacement queue requires a separately reviewed epoch.

The actual V4 checkpoint, supervisor closure and closed lock do not contain schema-version fields. V2 checks their existing field formats rather than adding fictitious required schema tags. The queue's existing schema is `s6d_approved_job_queue_v1`.

The supervisor result must be FINISH54/54, bind that checkpoint and have no active job. Owner lock must be released, hardware restoration resolved, and owned keep-awake state requested/owned/restored with RESTORED status. The closed census must be complete, stopped, error-free, unlatched, nonpending/nonstale, cover the exact original three shared roots and remain within physical40GiB. The offline80 exception is not used.

`closed_owner_lock` is the actual immutable `CLOSED_LOCK_<owner_nonce>.json` emitted by V4. Its nonce must match the selected `SUPERVISOR_CLOSURE_<owner_nonce>.json`; run/queue and a finite positive PID/creation pair must agree. The bank root's unique `closed_instances` must include all54 completed child instances and that final supervisor (55 required distinct pairs). Additional explicitly closed historical owner instances are permitted, so prior failure/restart history is preserved. Root still validates physical restoration/full case evidence separately and proves actual owner absence before producing this acceptance; the later existing allocation inventory rechecks absence. No paused or partial bank receipt can satisfy the new gate merely because its current owners have stopped.

All pre-existing C12 and Tk8 prerequisite checks remain unchanged. Tk follows complete bank closure. HOST2 additionally follows accepted full-source/Tk-view/owner closure. Actual native/session closure remains required before serial892's separate admission. General recording-overlap permission does not change this conservative root allocation.

## Focused synthetic checks

`s6d_tk_host_admit_checks_v2.py` exercises only F1 with tiny injected synthetic metadata matching the existing V4 shapes. It checks valid54 and missing phase,53 completed, active/paused state, partial supervisor result, missing/mismatched immutable lock, missing child/supervisor identities, open/stale/incomplete census, altered shared roots/cap and unresolved keep-awake/hardware. It makes no actual inventory, disk-free, current physical-state, model, audio or device call. Original V1 checks are preserved and not rerun.

PowerShell / Anaconda PowerShell Prompt:

```powershell
$sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\anaconda3\python.exe' -B "$sim\scripts\s6d_tk_host_admit_checks_v2.py" --source-root "$sim\scripts" --output 'G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\tk_host_admit_v2\checks_v3'
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_tk_host_admit_checks_v2.py" --source-root "%SIM%\scripts" --output "G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\tk_host_admit_v2\checks_v3"
```

The output is a fresh G-only `RECEIPT.json`; existing fixtures are never overwritten. No environment activation or package installation is needed.

The first synthetic run is retained in `checks_v1`, including its failed receipt and original tested check source. Two tests mutated a detached `phase` object after the parent fixture had been restored from a deep copy; they therefore failed to present their intended invalid input. The corrected checks access the current `bank['phase_closure']` directly. The admission source did not change for this fixture correction. A repeat run must use a new output suffix.

The second run passed25 on the initial draft. Before freezing, the saved actual launch receipt established the exact G-drive state path; the final source pins that path and the final26-check run adds rejection of the draft's unlaunched R-drive path. The initial source/build/check files are retained under `tk_host_admission_source_v2/draft_before_actual_state_binding`; neither earlier receipt is final source acceptance.

## Future root preparation and admission commands

These are command forms only; they have not been executed by this proposal. After independent/root source review, root may create fresh V2 plans without touching the V1 preparation:

```powershell
& 'C:\Users\amiri\anaconda3\python.exe' -B "$sim\scripts\s6d_tk_host_admit_v2.py" --prepare "$sim\reports\S6D\20260913T195357Z\runner\tk_host_admission_preparation_v2"
# Only after actual whole54 physical-bank and all other predecessor acceptance:
& 'C:\Users\amiri\anaconda3\python.exe' -B "$sim\scripts\s6d_tk_host_admit_v2.py" --admit tk8 --root-review '<actual exact root review path>' --root-review-sha256 '<actual SHA256>'
# HOST2 uses --admit host2 after separate actual Tk8 full-source/UI/owner acceptance.
```

```bat
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_tk_host_admit_v2.py" --prepare "%SIM%\reports\S6D\20260913T195357Z\runner\tk_host_admission_preparation_v2"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_tk_host_admit_v2.py" --admit tk8 --root-review "<actual exact root review path>" --root-review-sha256 "<actual SHA256>"
```

Admission outputs remain `ADMISSION_PREFLIGHT.json`, `ROOT_ADMISSION.json`, and `APPROVAL.json` last in the unchanged native group directory. V4 validation and launch remain separate existing root steps; this helper starts no models or hardware. Original deadline2026-09-16T19:53:57Z and45-minute closeout reserve are unchanged.
