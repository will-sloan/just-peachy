# Fresh N6g C12 queue preparation and separate root admission

Purpose: replace the failed N6f C12 execution with an additive, source-bound twelve-cell proposal using root-accepted N6g. Existing old queue, source, event failure and zero accepted jobs remain immutable. Scientific cells, profiles, A15 gallery, original E/C/Q partition, source PCM/proofs, model state/windows, source pacing, three-journal hashes, strict bb1b, inner-loop29ccc, protocol874d, V4fdff,40GiB roots/cap, C50/G75 floors, timeouts and campaign deadline are preserved.

Inputs are exact pinned original24 authority proposal/adoption, source-only root acceptancecb312b7c, corrected freeze921c, independent7937 and failed runtime closureec5. Preparation reads bounded source/metadata, never audio/model/vector/event journals or process state. It maps all old application source guards to the48 corrected copies, N6f to N6g and manifest to exactede8. Every job additionally guards the actual root source adoption, corrected freeze, independent review, old zero-credit closure, this helper/README and original authority provenance. Original required guards not replaced remain. Actual child argv retains V4's script-at-argv1 convention; no injected `-B` before the script.

`--prepare` creates only fresh `runner/beam_C_queue_proposed_v3/{QUEUE.json,APPROVAL_PROPOSAL.json,PREPARATION.json}`. Approved hashes remain empty. G protocol/state is `G:/Just_Peachy_S6D/20260913T195357Z/runner/beam_C_queue_proposed_v3`; native outputs remain the exact fresh paths in acceptedede8 (`application/beam_C_collection_source_origin_v1`). It creates no G runtime directory, approval, model, process or hardware action.

After root inspects the exact prepared queue/job hashes, it may create a separate review with status `ROOT_ACCEPTED_EXACT_N6G_C12_QUEUE_FOR_ADMISSION`, root `owner_thread_id`, run_id, exact `preparation`, `queue`, `source_adoption`cb312 and `prior_runtime_closure`ec5 bindings, `allow_admission=true`, `physical_supervisor_overlap=false`. No such review is created here. Root then invokes `--admit` with its exact path/hash. Admission verifies the original immutable failure/checkpoint/closed-lock/keep-awake/census, fresh current PID+creation absence and complete no-overlap process inventory, disk floors and original closeout deadline. Other Python workloads reject except exact known H2 script; physical/control markers reject even outside Python. No process is terminated. All source guards and V4 queue predicates pass before ROOT_ADMISSION/APPROVAL files are written. An occupied output/authority path refuses reuse.

No physical supervisor overlap is allowed during the C12 allocation. Admission rejects existing physical/control processes in code. Root must also withhold subsequent physical/other NN launches until C12 closes; this helper does not modify physical owners or invent a shared runtime mutex. Per-job immutable source guards and unchanged V4 full census/resource/deadline/closure checks remain active. Approval creation is not a model launch; root separately validates actual authority and launches one supervisor.

PowerShell (root only, after source review):

```powershell
$sim='C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' "$sim\scripts\s6d_C12_root_admit_v2.py" --prepare
# Inspect fresh PREPARATION/QUEUE; issue a separate exact root queue-review receipt first.
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' "$sim\scripts\s6d_C12_root_admit_v2.py" --admit --root-queue-review '<actual reviewed receipt>' --root-queue-review-sha256 '<actual SHA256>'
```

Anaconda Prompt or Command Prompt:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" "%SIM%\scripts\s6d_C12_root_admit_v2.py" --prepare
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" "%SIM%\scripts\s6d_C12_root_admit_v2.py" --admit --root-queue-review "<actual reviewed receipt>" --root-queue-review-sha256 "<actual SHA256>"
```

Admission reconstructs the complete preparation record, including scope, limits, completion gates, manifest/helper/runner, protocol/state paths, all original authorities and literal hashes. A matching review cannot silently alter descriptive completion or allocation claims. The exact serialized approval binding is included in ROOT_ADMISSION; APPROVAL is published last after all semantic checks.

Do not execute either production phase before root review. Focused fixtures exercise the pure transform, closure and allocation decisions, actual prepare orchestration, and intercepted admit publication with fake process inventory; they create no production metadata/authority or actual process query. [Fixture README](README_S6D_C12_ROOT_ADMIT_CHECKS_V2.md) records reproduction. The80GiB exception and future892 acceptance are outside this C12 change.
