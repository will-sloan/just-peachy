# Admission and fresh-evidence family rescue

Purpose: register24 bounded, explicitly outcome-informed policy configurations addressing the observed N00 clean-support/commit starvation. Fourteen additional N01 structural-family conditions complete all eight cue-off/real-cue pairs using the existing two normalized controls. Eight N00 controls lower only the estimator-support admission floor; two N01 controls permit short prototype updates. No model weights or canonical recordings change.

Inputs: completed N00 panel results, inspected N01 functional smoke, immutable V1 design and V2 actual registry. Outputs: ADMISSION_AND_FAMILY_RESCUE_V1.json, C117–C140 profiles and EFFECTIVE_PROFILE_REGISTRY_V3.json. Positive unique clean duration remains necessary for commitment even when zero-support proposals are permitted. All prior settings/results remain preserved. These configurations are exploratory; no pristine held-out-test or angle benefit claim is made.

PowerShell from repository root:

```powershell
& '.\.edge-speech-env\python.exe' '.\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6c_rescue_design.py'
```

Anaconda Prompt / CMD:

```bat
".edge-speech-env\python.exe" "XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6c_rescue_design.py"
```

Freeze a new execution epoch with the resulting explicit V3 registry before execution. Reuse unchanged prior native evidence only through the compatible-source dependency checks. Actual six-varied-scene native family checks, all240 retained confirmation and paced/endurance acceptance remain separate requirements.
