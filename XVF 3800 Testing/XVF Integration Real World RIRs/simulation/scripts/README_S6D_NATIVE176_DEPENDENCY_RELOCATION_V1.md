# Six-path native176 scoring correction proposal

`s6d_native176_dependency_relocation_v1.py` prepares a metadata-only correction for the actual scorer exit1 caused by exact dependency paths differing. The six loaded files have the same ordered byte lengths and SHA256 as the original frozen files:113,687 bytes total. The accepted scorer31408 and every4260 scientific calculation remain unchanged. Original failed execution92d189 and specbfaecace remain immutable.

Inputs are pinned parent spec, failed execution and f92bca path diagnosis; the unchanged actual176 VALIDATION885634; and both old/new copies of only six small source files. The helper reads no native audio, journals, transcript/result files or model assets. It does not import a scorer/metric backend, run a process, touch hardware, create approval or retry scoring.

Outputs under a fresh `--output` in R/application: `SCORING_INPUTS_PROPOSAL.json`, `EXACT_ALLOWED_CHANGE.diff` and `PREPARATION_RECEIPT.json`. All original fields remain identical except six dependency paths, explicit correction provenance and a deliberately non-runnable proposal status. Jobs176, comparisons176, four immutable manifests, original171/fresh5 selection, reference/gallery/source bindings, VALIDATION, root build provenance, scorer and original uncreated `output_root` are preserved. Native source scans are not repeated. The diff contains only the allowed changed projection; the helper separately asserts full remaining JSON equality.

The proposal is not executable: its status is `PROPOSED_DEPENDENCY_RELOCATION_NOT_AUTHORIZED`. Root must separately review the exact proposal and explicitly create/review a final accepted spec hash before any retry. This helper has no approval, activation or execution action. No existing file/directory is deleted, overwritten or repaired in place.

PowerShell:

```powershell
$sim='C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\anaconda3\python.exe' -B "$sim\scripts\s6d_native176_dependency_relocation_v1.py" --output "$sim\reports\S6D\20260913T195357Z\application\native176_dependency_relocation_v1"
& 'C:\Users\amiri\anaconda3\python.exe' -B "$sim\scripts\s6d_native176_dependency_relocation_checks_v1.py" --source-root "$sim\scripts" --output 'G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\native176_dependency_relocation_v1\checks_v1'
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_native176_dependency_relocation_v1.py" --output "%SIM%\reports\S6D\20260913T195357Z\application\native176_dependency_relocation_v1"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_native176_dependency_relocation_checks_v1.py" --source-root "%SIM%\scripts" --output "G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\native176_dependency_relocation_v1\checks_v1"
```

Use fresh output suffixes. Focused fixtures mutate only small in-memory metadata and prove foreign/reordered/changed source content, native-item/reference/validation/output changes and accidental runnable status fail. They do not repeat scoring or any176 native audit.
