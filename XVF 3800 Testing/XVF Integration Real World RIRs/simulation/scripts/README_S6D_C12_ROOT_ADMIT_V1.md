# Exact C12 collection admission

Purpose: create root authority for the existing, independently reviewed twelve-job calibration collection queue after native171+5 runtime closure. This helper changes no queue, scientific input, timing bound, selector, model or hardware setting. Inputs are the exact prepared proposal, source adoption, 24 authorities, previous runtime closure, fresh output paths and current Python process/disk evidence. Outputs are fresh `ROOT_ADMISSION.json` and `APPROVAL.json` under `reports/S6D/20260913T195357Z/runner/beam_C_queue_proposed_v2`. Re-running refuses existing authority.

The root reviews actual literals before running this helper. It verifies the exact queue with the unchanged V4 validator. This is admission only: it does not run models, scan payload trees, control hardware or terminate processes. The unchanged supervisor must perform its fresh full payload/resource preflight before starting any child. Prior native176 scientific validation and scoring remain pending; this selector-disabled collection depends on runtime slot closure, not a retention result. H2 maintenance is recorded and left unchanged. Separate physical bank capture and non-NN validation/scoring may overlap only as explicitly recorded in the admission; other NN/Tk/HOST/diagnostic work may not overlap.

PowerShell / Anaconda PowerShell:

```powershell
$sim='C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B "$sim\scripts\s6d_C12_root_admit_v1.py"
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6d_C12_root_admit_v1.py"
```

Then independently inspect the actual admission and approval, use the exact prepared V4 `--validate-only` command with the resulting approval hash and G supervisor state, and separately launch one hidden supervisor. The queue's `README_RUN.md` records the actual launch command once launched. Never use the generic physical launcher: it would choose a different state directory. No full-study completion follows from collection FINISH alone.
