# Literal native queue preparation

Purpose: turn the file-only `native_execution_preparation_v1` manifests into exact held production queues after the additive full-source protocol wrapper is independently reviewed. The helper creates no approval, worker, model, real Tk window, supervisor or device activity. Root remains the launch authority. It preserves the prepared scientific order, profiles, galleries, waveform PCM, helper/application epochs and fresh G outputs.

Inputs to `s6d_native_queue_prepare_v1.py`: `--preparation` (the exact earlier preparation directory), `--wrapper` plus `--wrapper-sha256`, `--wrapper-review` (the independent closure receipt), and a fresh `--output` directory directly under `R/runner`. The accepted censusV4 runnerfdffb4ca and evidencebb1b must be unchanged; the root acceptance receipt is checked. Final manifests add explicit protocol runner/evidence/wrapper bindings without altering the engine or scientific jobs. Prepared drafts remain intact.

Outputs: separate `native176`, `tk8`, `host2` queues with literal argv and exact expected completion artifacts; `APPROVAL_PROPOSAL.json` files containing an empty approved-job list and exact proposed hashes; final manifests, remapped prospective scorer/reference bindings, `QUEUE_PREPARATION_RECEIPT.json`, and runnable root-adoption/validation instructions in `README_RUN.md`. Generated `Invoke-Native-Queue.ps1` accepts a group and defaults to validate-only; its separate `-Execute` switch is for a later root-authorized launch. It requires actual root approval/evidence and validates the queue/runner hashes first; CMD/Anaconda commands are included in the output README. All186 job outputs must remain fresh. The builder pins the original8 model declarations without repeatedly hashing multi-GiB assets; constructors retain that responsibility. Small code/manifest/profile/gallery sources are bound by the supervisor, and the wrapper validates actual PCM/full journals/dispatch before protocol COMPLETE.

Every queue requires the accepted wrapper's `full_source_evidence_validated`, `completion_audit`, exact affinity, matching manifest/helper/PID creation, stop=false, closed observers and semantic worker/consumer drain. Tk additionally requires `tk_view_evidence_validated` and the exact owned-resource closure. C50/G75GiB floors, shared40GiB payload cap, the original campaign deadline and45-minute closeout reserve remain unchanged. Two uninterrupted HOST sessions are distinct from physical900-second captures. Source/owner/protocol closure and later scientific scoring remain separate admissions; a missing result or a successful process exit is insufficient.

PowerShell (substitute the exact independently reviewed wrapper path, SHA and receipt; no model execution):

```powershell
$sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
$report = "$sim\reports\S6D\20260913T195357Z"
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B "$sim\scripts\s6d_native_queue_prepare_v1.py" --preparation "$report\application\native_execution_preparation_v1" --wrapper '<exact reviewed wrapper.py>' --wrapper-sha256 '<exact SHA256>' --wrapper-review '<exact independent review.json>' --output "$report\runner\native_execution_queue_preparation_v1"
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
set "REPORT=%SIM%\reports\S6D\20260913T195357Z"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6d_native_queue_prepare_v1.py" --preparation "%REPORT%\application\native_execution_preparation_v1" --wrapper "<exact reviewed wrapper.py>" --wrapper-sha256 "<exact SHA256>" --wrapper-review "<exact independent review.json>" --output "%REPORT%\runner\native_execution_queue_preparation_v1"
```

Use a new output suffix for deliberate regeneration; never overwrite the completed queue or mutate its scientific selection. The output `README_RUN.md` supplies concrete commands and queue hashes. Root must create actual approval after combined review; this builder never copies proposed hashes into an approved list. Later root launch runs supervisor validate-only first, then the separately authorized execution command.
