# First S6D physical capture admission

Purpose: materialize one exact reviewed P_INPUT_QA6 tagged-input sentinel after the real user confirmation and successful post-reset preflight. No playback is started by this helper. It binds the accepted V4 owner/bridge/transport/vendor graph, S4.5 DSP recipe, unity-input source, actual safety acknowledgement and inherited supervisor limits. It authorizes only8source seconds (12.3413125 charged seconds with guards/padding); controls, C calibration and the main bank require review of actual integrity/restoration first.

Inputs: physical_preparation_review_v2 immutable source/proposals, accepted root source review, fresh preflight/confirmation/reset history, and original supervisor campaign/storage policy. Outputs: fresh reports/S6D/20260913T195357Z/runner/first_capture_queue_v1 safety/plan/authorization/queue/approval/review JSON. Existing admission or physical ledger causes refusal; preserve previous evidence. Source snapshot basenames must be unique. Pre-reset unreadable DSP state is not claimed restored; the owner records and restores its actual new batch initial state.

PowerShell:

```powershell
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6d_first_capture_admit_v1.py'
```

Anaconda Prompt / CMD (standard library; no installation):

```bat
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B "C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6d_first_capture_admit_v1.py"
```

Root next validates the literal queue with the reviewed runner --validate-only and exact printed queue/approval SHA256 values, then runs the same supervisor command with --keep-awake. The actual command and PID/creation-time record will be written to this queue folder's README_RUN.md and ROOT_LAUNCH_V1.json. The supervisor runs with Edge Python, but the hardware child explicitly uses C:/Users/amiri/anaconda3/python.exe. No Windows-default audio device is permitted; the recorder selects exact XVF WDM-KS endpoints after reset. Hardware processes must never be killed by the supervisor. COMPLETE, exact four-MIC recovery, actual restoration and owner closure are all required; a running PID or exit0 is insufficient.
