# Root-authorized C capture metadata adoption

Purpose: materialize exactly12 actual C-case route, catalog and C-partition records after root explicitly authorized collection-only adoption. `s6d_C_capture_adopt_v2.py` pins root physical acceptance87721aa and inactive proposal freezef3b0a594. Input `--output` must be a fresh direct child of the S6D report root. The helper verifies each original case/configuration/source identity, exact inactive proposal and per-case route/tail evidence already bound by root acceptance. Outputs:12 new route JSON files, C_CALIBRATION_PARTITION.json, CAPTURE_CATALOG.json and ADOPTION_RECEIPT.json. Original receipts/proposals/audio are immutable; no hardware, neural model, inference, calibration or playback occurs.

Route PASS is narrowly scoped to actual verified routing, common decoded frame origin and observed guard-tail adequacy. It does not establish exact per-focus DSP delay, every-event source survival, identity, clean separation or level guarantees. All original per-stream limits and root limitations remain in each new record. C/E/Q disjointness and12 actual original bindings are retained. Gold-time projection and selector thresholds explicitly remain unapproved. This adoption is not a model-execution queue or permission to launch.

PowerShell:

```powershell
$sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' -B "$sim\scripts\s6d_C_capture_adopt_v2.py" --output "$sim\reports\S6D\20260913T195357Z\physical_C_collection_admission_v2"
```

Anaconda Prompt / CMD:

```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" -B "%SIM%\scripts\s6d_C_capture_adopt_v2.py" --output "%SIM%\reports\S6D\20260913T195357Z\physical_C_collection_admission_v2"
```

Use only the root-authorized scope above. This is a one-time materialization into fresh outputs; a different acceptance/proposal requires a separate reviewed epoch. Existing exact accepted beam helpers and pending/active native176 sources are not changed.

V2 corrects inherited acquisition status and accounting fields. It reports all12 already-closed physical C captures and sums their exact charged_playback_seconds from original attempt timings. It creates zero new physical attempts/charges. Source case and control entries now carry both actual profile acquisitions; original inactive proposals and V1 remain unchanged. All timing-label, threshold and model-execution approvals remain separate and absent. The receipt freezes the helper and its metadata dependencies. All changes are metadata only.
