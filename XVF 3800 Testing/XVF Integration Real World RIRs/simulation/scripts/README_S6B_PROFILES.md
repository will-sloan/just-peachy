# S6B compiled profile registry

`s6b_profiles.py` turns the reviewed design into strict actual application profiles and exact upstream neural recipe groups. Every changed tracker field must be operative for its mode. It preserves the original profile IDs with documented replacements, and explicitly enumerates four limited challenge diagnostics in addition to the forty core configurations.

Inputs: the reviewed design registry and recipe proposal, actual application profile/tracker API. Outputs: `profiles/*.json`, `EFFECTIVE_PROFILE_REGISTRY.json`, `NEURAL_RECIPE_REGISTRY.json` and `EFFECTIVE_PROFILE_DIFFS.json`. Profile validation does not claim an inference or a successful method effect.

PowerShell:

```powershell
& 'C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe' 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6b_profiles.py'
```

Anaconda Prompt / CMD:

```bat
"C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe" "C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation\scripts\s6b_profiles.py"
```

Compile before freezing an execution epoch. Do not rerun the compiler while its profile/index files are inputs to active jobs. Eight conceptual recipe families have more than eight exact execution identities because changed admission, audio and stateful ASR histories require fresh inference. The initial eight recipes are profiled before admitting the additional bounded branches. Cues disabled in tracker-only counterparts do not create a different upstream cache; endpoint/cadence cue effects do.
# Reviewed duration contrast

B20 and its B20_C1 companion use a 1.0 s embedding window at the same 0.25 s hop as B18/B18_C1. The 2-by-2 duration/commitment comparison therefore holds embedding cadence fixed. This correction precedes all campaign inference; epoch1 was frozen but never executed, and epoch2 is the admitted execution snapshot. B24/B25 remains an explicitly bundled spatial scheduling plus tracking comparison, so its accuracy delta is not attributed solely to scheduling.
