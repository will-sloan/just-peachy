This candidate repairs three source-only calibration findings: omitted unresolved auto competitors, unscoped root support acceptance, and per-gate fits without joint selector support. It preserves the frozen V1 helper9165da50 and collection helper6f716290. It never invokes models, hardware, audio playback, per-beam alignment or a threshold grid. Actual C extraction and any selector enablement remain pending root admission.

Purpose and inputs: `s6d_beam_calibration_v2.py extract` reads one exact closed C collection manifest/job, wrapper completion, full multistream result/audit, and root-accepted conservative captured C support. Full frozen V1 frame/journal/native-event checks remain mandatory. It requires the exact original A15 gallery8042fbec and its actual15 profile IDs, the original admitted C partition with status ROOT_ACCEPTED_CAPTURED_C_FOR_COLLECTION_ONLY, the existing profile/window/clean gate, and exact beam settings. Every candidate must join a preceding actual mature clean identity event by capture/route/stream/event/identity/source clock. Source times must lie on16k frames and inside the full capture.

A candidate is never discarded merely because its recognized identity is unresolved. All actual probe candidates remain in the feature receipt. Runtime-ineligible, unknown, nonmember, overlapping and ambiguous populations remain explicit. For auto, every fresh eligible same-window finite correlation remains a competitor, including unnamed identities. Unresolved identity or ambiguous source support has label ambiguous, never invented negative truth. Known original C nonmembers may be negative named-association evidence when a wrong confirmed enrolled name is emitted. Original possible input identity still does not establish that every focus preserved that speaker or identify an exact acoustic auto target.

Root support acceptance is acyclic. The support JSON retains its `root_acceptance` file binding. Its payload digest is SHA256 of UTF-8 canonical JSON with sorted keys, separators comma/colon, allow_nan=false, excluding only the top-level `root_acceptance` field. The separately issued root JSON must use:
```json
{
  "schema": "s6d-C-support-root-acceptance.v1",
  "status": "ROOT_ACCEPTED_CONSERVATIVE_SOURCE_SUPPORT",
  "Q_used": false,
  "disjoint_from_E_Q_verified": true,
  "acceptances": [
    {
      "case_result": {"path": "<exact original case>", "bytes": 0, "sha256": "<actual>"},
      "partition": {"path": "<exact original C partition>", "bytes": 0, "sha256": "<actual>"},
      "gallery": {"path": "<exact A15 gallery>", "bytes": 0, "sha256": "<actual>"},
      "support_payload_sha256": "<actual canonical payload digest>",
      "C_source_ids": ["<sorted exact source IDs present in accepted spans>"]
    }
  ]
}
```
This is a schema illustration, not an acceptance file. Root must independently accept the actual conservative projection and its limitations. The helper checks status/schema/noQ/disjointness and requires exactly one identical record for the current payload/case/partition/gallery/source IDs. A valid hash of an unrelated or unaccepted JSON fails. Changing bounds, exclusions, source maps, clock receipts or any other support payload field invalidates the acceptance. No authority writer is provided.

`propose` re-reads the exact closed-source feature provenance and reconstructs the feature/probe populations before fitting, rejecting changed rows or incompatible gallery/profile/beam-settings/capture-profile/stream/selected-ID contexts. MAIN and SCAN cannot be pooled. It uses the same fixed threshold rule: each metric exceeds the maximum observed known-negative and ambiguous metric by1e-6. Ambiguity is a conservative guard, never counted as a negative class. Every metric still needs two distinct original C source IDs in each known positive/negative class and two retained positive sources. Repeated windows or repeated room placements of the same clip cannot inflate this count.

All five proposed thresholds then undergo joint replay through the exact `BeamSelector.choose` function AST from frozen beam source deaf0af8. This executes no model code. Auto waveform similarity is replaced only by the already measured actual same-window correlation recorded by the reviewed collection helper; there is no new waveform calculation or search. Selected duplicate resolution stays false, so no duplicate waveform/vector decision is used or fitted. All runtime source-age/availability/identity/source-span/competition/threshold gates remain the original function.

For each selector separately, joint replay requires at least two distinct original C source IDs with retained positive outcomes, at least two negative-challenge sources under the fixed zero-gate baseline rejected or corrected by the proposed gates, and at least two retained positive sources with actual identifiable positive/negative competition. Any final negative or ambiguous winner blocks an enablement candidate. A per-metric positive count cannot substitute for these joint outcomes. If actual C lacks support, status remains CALIBRATION_SUPPORT_INSUFFICIENT_SELECTORS_DISABLED; no nominal threshold or positive count is supplied.

Outputs: V2 feature JSON with every probe/candidate and conservative truth state, or a proposal with per-gate and joint populations. Even PROPOSED_C_ONLY_JOINT_SUPPORTED_PENDING_ROOT_REVIEW has selectors_enabled=false and thresholds_accepted=false. Root must independently review scope/negative evidence and issue a separate exact-context engine-compatible ACCEPTED_C_ONLY calibration receipt before runtime enablement. This helper never creates that receipt.

PowerShell / Anaconda PowerShell Prompt — tiny fixtures only:
```powershell
$Sim = 'C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation'
& 'C:\Users\amiri\anaconda3\python.exe' -B "$Sim\scripts\s6d_beam_calibration_checks_v2.py" --output 'G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\beam_calibration_v2_final_checks'
```

Anaconda Prompt / CMD — tiny fixtures only:
```bat
set "SIM=C:\Users\amiri\Documents\GitHub\just-peachy\XVF 3800 Testing\XVF Integration Real World RIRs\simulation"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_beam_calibration_checks_v2.py" --output "G:\Just_Peachy_S6D\20260913T195357Z\review_fixtures\beam_calibration_v2_final_checks"
```
The18 tiny tests cover semantic root scope/digest/noQ checks, unresolved auto competitors, ambiguous bounds, stale/future eligibility, exact joint selected/auto positive and negative cases, per-gate-only false support, one-source inflation, capture scope and event-ID/stream joins. Inputs are in-memory synthetic probes and small clearly synthetic root JSON files. Outputs are TESTS.log, RECEIPT.json and those fixture JSON files, all on G. The earlier16-check draft and tested source snapshot remain preserved. There are no actual C features, model outputs or accepted thresholds in these fixtures.

After separate root source/input admission only, the model-free extraction/proposal command forms are:
```powershell
& 'C:\Users\amiri\anaconda3\python.exe' -B "$Sim\scripts\s6d_beam_calibration_v2.py" extract --manifest '<exact closed C manifest>' --manifest-sha256 '<SHA256>' --job-id '<literal C job>' --completion '<actual closed COMPLETION.json>' --support '<accepted conservative captured C support.json>' --output 'G:\Just_Peachy_S6D\20260913T195357Z\application\beam_C_features_v2\case.json'
& 'C:\Users\amiri\anaconda3\python.exe' -B "$Sim\scripts\s6d_beam_calibration_v2.py" propose --features '<first exact compatible V2 feature.json>' '<second exact compatible V2 feature.json>' --output 'G:\Just_Peachy_S6D\20260913T195357Z\application\beam_C_features_v2\THRESHOLD_PROPOSAL.json'
```
```bat
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_beam_calibration_v2.py" extract --manifest "<exact closed C manifest>" --manifest-sha256 "<SHA256>" --job-id "<literal C job>" --completion "<actual closed COMPLETION.json>" --support "<accepted conservative captured C support.json>" --output "G:\Just_Peachy_S6D\20260913T195357Z\application\beam_C_features_v2\case.json"
"C:\Users\amiri\anaconda3\python.exe" -B "%SIM%\scripts\s6d_beam_calibration_v2.py" propose --features "<first exact compatible V2 feature.json>" "<second exact compatible V2 feature.json>" --output "G:\Just_Peachy_S6D\20260913T195357Z\application\beam_C_features_v2\THRESHOLD_PROPOSAL.json"
```
These illustrative extraction commands have not been executed. All outputs must use fresh G-only names; never overwrite prior attempts. Run maintained SIM/scripts helpers or a reviewed frozen directory containing the exact V1 dependencies. All source/authority changes require a new reviewed epoch. C50/G75,40GiB shared payload and the unchanged campaign deadline still apply to any later root-admitted experiment. This candidate changes no runtime selector, model, gallery, active queue or physical owner.

