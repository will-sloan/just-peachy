// Copyright 2022-2023 XMOS LIMITED.
// This Software is subject to the terms of the XCORE VocalFusion Licence.

/***************************************************/
/* AUTOGENERATED. DO NOT MODIFY!                   */
/* Generated using template {{template_file}} */
/***************************************************/

#pragma once
#include "enum_str_map.h"

// File containing functions that take a value and return the string key for that value from the enum value to string map
// Function name is of the form: print_enum_{cmd_name}_{parameter_index}
// Value to key map that is referred to is {cmd name's super_print_parameters[parameter_index]}_value_to_key_map
// map[value] is read to get the enum string this value maps to

{% for cmd_print_info in render_list %}
{% for enum_print_info in cmd_print_info['enum_param_list'] %}
void print_enum_{{cmd_print_info['cmd']|lower}}_parameter_{{enum_print_info['param_index']}}(uint8_t val)
{
    if({{enum_print_info['str_map']}}_value_to_key_map.find(val) == {{enum_print_info['str_map']}}_value_to_key_map.end())
    {
        std::cerr << "Invalid value " << static_cast<int>(val) << " not part of the enum \n";
        exit(-1);
    }
    else
    {
        std::cout << {{enum_print_info['str_map']}}_value_to_key_map[val] << "[" << static_cast<int>(val) << "] ";
    }
};
{% endfor %}
{% endfor %}
