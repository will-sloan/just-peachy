// Copyright 2023 XMOS LIMITED.
// This Software is subject to the terms of the XCORE VocalFusion Licence.

/*********************************/
/* AUTOGENERATED. DO NOT MODIFY! */
/* Generated using template {{template_file}} */
/*********************************/

{% for pin in render_list.values() %}
{% if pin['active_level'] not in (0, 1) %}
{{ "active_level can only be 0 or 1. Check gpi_config.yaml file"/pin }}
{% endif %}
{# The intent of this is "if pin['event_config'] not in (e_gpi_interrupt_edge_t)" , but cannot get that information at template generation. #}
{% if pin['event_config'] not in ("EdgeNone", "EdgeFalling", "EdgeRising", "EdgeBoth") %}
{{ "event_config must be one of EdgeNone, EdgeFalling, EdgeRising, or EdgeBoth. Check gpi_config.yaml file." }}
{% endif %}
.active_level[{{loop.index0}}] = {{pin['active_level']}},
.event_config[{{loop.index0}}] = {{pin['event_config']}},
{% endfor %}