# Four-way one-cell readiness review — v1

The observer, exact ASR comparisons and available archive checks pass for one identical 44.6954375-second source cell across D0/E0, D1/E0, D0/E1 and D1/E1. This is **NOT_FULL_SCREEN**: 4/384 cells and one matched four-way cell. It permits the root-owned full run to proceed from this narrow gate; it does not establish broad diarization, naming, GUI or deployment quality.

All three candidates pass all six comparisons against D0/E0: exact raw observations, raw final events, source dispatch/reset sequence, final utterance raw text, final raw word order and formatted final utterance text. The shared signatures contain 28 raw observations, three raw final events, 462 source dispatch/reset entries, three final utterances and 32 raw words. All 60 gallery-condition caption replays preserve raw and formatted words; all four baseline clones agree with actual final Controller rows. These are causal shadow conditions on actual emitted windows, not 60 independent model or GUI runs. First-visible, first-final and latest shadow states remain separate; actual final Controller text is authoritative.

All four recorders drain with equal accepted/completed event counters. Observer queues have no unmatched embedding/decision IDs or recorded errors. Matches are 26, 15, 26 and 15 in D0/E0, D1/E0, D0/E1 and D1/E1 order. Six D0/E0 and two D0/E1 actual decisions have no resolved track; those remain explicit rather than being counted as unmatched callbacks.

Three v4 runtime results contain a passing archive gate: all 715127 source samples, no loss/error, closed worker, empty item/byte queues and equal archive accepted/completed counters. D0/E1 v3 lacks that added result field and counter receipt; its final snapshot passes the available sample/loss/closure/queue checks. The limitation remains in the public receipt.

Both D0 variants fragment this single-speaker cell. Observed track-ID counts are six for D0/E0 and ten for D0/E1, and neither keeps either of two returning turns consistent. Their cpWER values are 1.40625 and 1.46875; D1/E0 and D1/E1 have one track, two consistent returns and cpWER 0.125. These descriptive one-cell failures are retained. No Q threshold was changed. D0 full activity DER remains unavailable; D0 counts use actual embedding-window support as a qualified proxy.

## Reviewed execution path

The parser requires `--source`, `--output`, `--manifest` and one of four `--combination` values. `--profile` defaults to `low_latency`; `--device` explicitly selects CPU or CUDA. `--limit 1` limits execution without redefining the original 96-job manifest denominator. The full run omits that limit and uses the common frozen v5 source. Source delivery stays at absolute source speed with the existing Controller and Sherpa/PnC paths. A new Controller/session resets scene state; only admitted model weights remain resident. The strict runtime manifest admits the eight audio-job fields and rejects changed gain, rate or reset behavior.

The bounded recorder handles actual stamped events after publication. The observer consumes the same actual embeddings and decisions without truth or inference. Current `s6d_text_ready` records provide the exact raw observation signature even though `research_asr_observation` is not recorded. The summary reconstructs flattened final rows by utterance/token order. It verifies admission, source-audio, checkpoint and evidence bindings before scoring.

The current runner closes owned pending commands and joins workers, fails unsafe cleanup, and requires the archive counters and source sample count to agree. Its admission now binds the supervisor source. Both runner I/O and supervisor use bounded two-second retries for transient Windows atomic-replacement sharing errors. These I/O changes were reviewed without launching a model. The root separately owns supervisor tests and common GUI acceptance.

## Source qualification

D0/E1 ran frozen v3; the other three ran v4. Their source hashes are retained individually. The application-code change from v3 to v4 adds D1 embedding archive provenance only; the D0/E1 path does not execute it. v4 to v5 changes only the inclusion of `release_tools/requirements-arm64.in` and `.lock` when generated bytecode is excluded. Application, vendor and config sources agree, as do all six shared UI files. Thus the receipt does not claim byte-identical smoke snapshots. All main factorial combinations must use the same final admitted v5 snapshot.

v5 frontend runtime SHA-256 is `6b1c960e1f31955e6d9dc93716ade0315182ea3ea0f922e5b2ee4e976d4db0a7`; common UI SHA-256 is `54c0283b8058978eca87dc9b0461a15addf804200856e41d2c83b55bff590456`. The machine receipt binds current runner, supervisor, observer, scorer and helper hashes and keeps the earlier audited runner hash separately. A later executable change needs another source review.

## Reproduce scoring without model calls

Inputs are the four existing private smoke `RESULT_INDEX.json` files, frozen private evaluator truth and their bound events/snapshots. Outputs are private `SCREEN_EVIDENCE.json` and redacted `SCREEN_SUMMARY.json`/Markdown under a distinct private output directory. Per-cell evaluator scores stay beside the private attempt. The commands intentionally omit `--require-complete` because this is a partial preflight. `PARTIAL` is the expected campaign status; only the per-cell readiness gate passes.

PowerShell, after setting `EvalDir` and `PythonExe` from this directory's README:

```powershell
& $PythonExe -B "$EvalDir\summarize_screen.py" --index 'G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D0E0-v4\RESULT_INDEX.json' --index 'G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D1E0-v4\RESULT_INDEX.json' --index 'G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D0E1-v3\RESULT_INDEX.json' --index 'G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D1E1-v4\RESULT_INDEX.json' --truth 'G:\Just_Peachy_N1\20260924_campaign\local\n2\evaluation\EVALUATOR_TRUTH.json' --out 'G:\Just_Peachy_N1\20260924_campaign\local\n2\evaluation\four-way-smoke-recheck' --public-out 'G:\Just_Peachy_N1\20260924_campaign\local\n2\evaluation\four-way-smoke-recheck\public'
```

Command Prompt / Anaconda Prompt, after setting `EVAL_DIR` and `PYTHON_EXE` from the README:

```bat
"%PYTHON_EXE%" -B "%EVAL_DIR%\summarize_screen.py" --index "G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D0E0-v4\RESULT_INDEX.json" --index "G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D1E0-v4\RESULT_INDEX.json" --index "G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D0E1-v3\RESULT_INDEX.json" --index "G:\Just_Peachy_N1\20260924_campaign\local\n2\smoke-D1E1-v4\RESULT_INDEX.json" --truth "G:\Just_Peachy_N1\20260924_campaign\local\n2\evaluation\EVALUATOR_TRUTH.json" --out "G:\Just_Peachy_N1\20260924_campaign\local\n2\evaluation\four-way-smoke-recheck" --public-out "G:\Just_Peachy_N1\20260924_campaign\local\n2\evaluation\four-way-smoke-recheck\public"
```

The minimal gate requires one completed identical source cell per combination, no cell/checkpoint/evidence failures, all 18 exact ASR comparisons, all raw/formatted clone comparisons, drained recorders/observers, matched emitted embedding/decision IDs and passing available archive checks. Quality scores are retained even when poor. Full completion instead requires all 96 × 4 cells and all full-screen gates; the separate eight-job regression population must also be scored independently.
