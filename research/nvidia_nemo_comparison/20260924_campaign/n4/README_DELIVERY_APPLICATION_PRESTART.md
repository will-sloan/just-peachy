# Invisible preparation of the source-delivery application variant

Purpose: `probe_delivery_application_prestart.py` and
`test_delivery_application_prestart.py` qualify actual Tk/Controller preparation
and closure for `paced_application_cell_v2.py` on all 16 implemented backend
rows. The same qualified journal source, E research roster, saved-file mode and
480 x 800 frontend are used. No FileSource, model, inference or device starts.

Inputs: the exact APPLICATION_DELIVERY_CHECK_V1 development qualification and
the existing source/context/private-process/UI qualifications; a fixed admitted
panel job supplies metadata only. The output must be a fresh private N4 folder.
The existing model-free writer lock, live D1 ownership/heartbeat/source checks,
CPU14 BelowNormal/GPU-off placement, private inventory and C:/G: disk reserves
apply. One extra invisible child shares helper CPU14. The healthy numerical
worker stays on CPU4. A 12-minute parent, four-minute child and 8-MiB output
bound apply.

The suspended child uses the existing exact-owner JobObject/private desktop
launcher, with script, executable, argument, nonce, parent, CPU and admission
bindings verified before test imports. It never switches to the user's input
desktop. Source/model acquisition is explicitly forbidden: model acquire and
ONNX session construction, Controller file/live starts, and FileSource.__new__
raise. Keeping the original __init__/start methods intact lets the new launch
adapter verify their exact source-code origins without starting them.

Four source-lineage tests and nine invisible GUI tests run. The latter retain
the existing viewport/journal-factory checks and prepare/close all 16 backends
using the new cell. Each requires unchanged GUI geometry, adaptation off,
no engine/consumer, a real non-fixture delivery adapter that was never installed,
zero source starts and PREPARED_WITHOUT_SOURCE_DELIVERY evidence. Controller
workers, Tk and owned process resources must close normally, with unchanged input
desktop and an empty owned JobObject.

Outputs: private source snapshots, reconstructed context, ADMISSION.json,
REGISTRATION.json, lineage_tests.txt, gui_tests.txt, per-backend PREPARED.json,
RESULT.json and delivery/CAPTURE.json, CHECKS.json, FACTORY_CHECKS.json, child
result and lifetime receipts, then RESULT.json or preserved FAILED.json.
PASS_DELIVERY_CONTEXT_AND_PRESTART_ONLY is preparation evidence, not the actual
source-run branch, deadline fidelity, latency, accuracy, resource-tier,
continuity, functional restart, N4 acceptance or any CM5 validation. The new
cell still needs an explicitly bound runner/planner/reviewer and real controlled
source runs. See README_APPLICATION_DELIVERY.md for its runtime interface.

PowerShell (always choose a fresh output suffix):

```powershell
$jpPython='C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe'
$jpCode='G:\Just_Peachy_N1\20260924_campaign\worktree\research\nvidia_nemo_comparison\20260924_campaign\n4'
$jpLocal='G:\Just_Peachy_N1\20260924_campaign\local'
& $jpPython -B "$jpCode\probe_delivery_application_prestart.py" run --output "$jpLocal\n4\delivery-application-prestart-v1"
```

Command Prompt and Anaconda Prompt (direct pinned interpreter):

```bat
set "JP_PY=C:\Users\amiri\Documents\GitHub\just-peachy\.edge-speech-env\python.exe"
set "JP_CODE=G:\Just_Peachy_N1\20260924_campaign\worktree\research\nvidia_nemo_comparison\20260924_campaign\n4"
set "JP_LOCAL=G:\Just_Peachy_N1\20260924_campaign\local"
"%JP_PY%" -B "%JP_CODE%\probe_delivery_application_prestart.py" run --output "%JP_LOCAL%\n4\delivery-application-prestart-v1"
```

The child subcommand is internal and requires the exact fresh registration; do
not invoke it directly. Keep failed attempts and private records intact. Publish
only code, documentation and redacted hash/qualification metadata.
