# N4 limitations and status taxonomy

IMPLEMENTED: bank preparation, separate twelve-composition wiring, established
scoring wrappers, paired uncertainty, strict coverage accounting, read-only
resource probe, content-addressed lossless archival, exact upstream inspector.

ACTUALLY_RUN: 480 waveform checks; twelve Controller selection/cleanup checks;
15 inherited model-free tests; 27 N4 tests; two existing N2-cell scoring smokes;
one lossless archive probe; dependency/source/queue binding checks. None is a
new N4 neural inference or a resource/latency qualification.

UNAVAILABLE: exact phonetic word timing (therefore tcpWER); physical CM5/XVF,
GPIO/camera/IMU tests; operational processed-query calibration; recorded complete
D0 activity in current Controller evidence. Raw reference text is source text,
not independent conversational punctuation gold. No inferred demographics.

FAILED AND RESOLVED IN PREPARATION: default MeetEval Windows build rejected
designated initializers; explicit C++20 compiled unchanged code. An empty-reference
JER test revealed upstream division by zero; unavailable rates plus retained
false-alarm counts pass the revised tests. No failed model run is hidden by these
preparation corrections. V1 catalog preparation omitted the updated expected-set
test; V2 changes that fixture and passes the 15-test suite, preserving V1.

NOT_TESTED / NOT_FINISHED: all N4 integrated cells, A1 integrated application,
D0/E1 association calibration, complete D0 activity recording, archive-aware
runner lifecycle, full mode/gallery contrasts, naming acquisition/exposure and
actual visible revisions, shortlist quality and worst cases, isolated cold/warm
resources and teardown, actual-GUI paced panels, 20-minute continuity, per-backend
Windows launch with loaded models, ARM64/CM5 performance and 2-GB total-system fit.
Resource sampling infrastructure is not resource data. All deployment tiers are
UNKNOWN; 1.5 GiB application plus 0.5 GiB system reserve is planning only.

READY_FOR_REVIEW on an upstream numerical job is not stage acceptance. N4's
current checkpoint cannot certify N2/N3 while they are still running/waiting.
Missing outputs remain NOT_TESTED in the full denominator. No arbitrary score,
model-family rejection, preferred winner, threshold transfer or default promotion.

The nine broad dependency clusters are highly unequal (72, 60, 57, 15, 13, 11,
10, 1, 1 scenes). Even cluster-aware intervals do not establish external validity.
The paced panel has one scene from each family and required regressions, but does
not represent every noise level. Full-bank condition analysis remains required.

Existing N2/N3 evidence and private source payloads remain in place. The tested
archive is a separate private copy; no deletion/cleanup bypass was performed.
Full-matrix disk feasibility needs a measured bounded lifecycle, not extrapolation
from one favorable compression example. G/C reserves and shared deadline remain.
