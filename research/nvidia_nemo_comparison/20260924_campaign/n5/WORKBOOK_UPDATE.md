# Proposed workbook update — N5 PARTIAL

N5 prepared a 310,867,595-byte private offline baseline bundle with eight exact
baseline assets and 13 ARM64 wheels; every member's hash was verified after ZIP
creation. ARM64 static verification covers 151 wheel ELF binaries. A genuine
CPU-only native ARM64 runtime was cross-built (six ELF files, armv8-a), packaged
with source pins/notices, and passed QEMU CLI/C-ABI loader checks. No model/WAV
smoke, target install, CM5 capacity, timing, thermals or physical hardware was
tested. The new runtime is an engineering artifact, not an accepted alternative.

Fixed an existing SSH installer transfer omission (runtime_lock.py); added an
isolated transferred-helper regression. Windows/Linux release checks and four
new package integrity/refusal tests pass. N3 admission bindings remain valid.
The original personal store, GUI files, active workers and Pi were not changed.

N2/N3 state is timestamped in N5_STATUS.json. N4 still has zero accepted full-bank
results; selected-composition releases, model parity, per-build GUI lifecycle,
resource tiers and final campaign closure remain pending. Notes retain scoped
N1 evidence plus explicit N5 tests and unresolved comparisons. No denoising/new
capture ran. The noisy-name failure cause is not established. Do not mark N5 or
the campaign complete from this checkpoint. Final reserve/deadline remain the
original shared campaign values. This proposal does not edit the master workbook.

During this continuation, N2 completed all 422 cells and final checks passed.
N3 v2 was stopped before neural work by an incorrectly frozen bytecode cache.
The selector was corrected and a regression added; nine queue tests pass.
Fresh v3 has 373 source files, zero bytecode, a verified 32-job plan and a hidden
supervised restart. V2 is preserved. N4's old v2 source derivative must be
regenerated from the eventual accepted N3 parent before comparison admission.
