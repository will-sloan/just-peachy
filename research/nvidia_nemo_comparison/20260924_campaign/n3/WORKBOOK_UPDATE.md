# Proposed workbook update — not applied to the master workbook

Stage N3: IN_PROGRESS, preparation checkpoint. Do not mark complete or promote
an N4 release from this document. User requested Astra / extra-high reasoning /
normal speed; those preferences are recorded, not inferred from throughput.

Implemented: exact-model native A2/A3 streaming adapter, actual A1 EOU reference,
A2/A3 NeMo references, continuous journal integration, model/configuration locks,
audio-only inference firewall, fixed source-paced and GUI panels, evaluator,
P0/P1 diagnostics and independent traceable finite ITN.

Actually run: exact official model downloads/hash checks; native C header ABI
compile; 24 model-free protocol/catalog/scoring/queue tests; WSL FST compilation
and exhaustive 110-form parity; Windows application of all 110 numeric forms
with trace and disabled-layer checks. No new N3 neural results yet.

Pending: N2 prerequisite; actual A0-A3 paired screen, CPU/reference/native
comparisons, real GUI/file tests, A1 portable streaming qualification, metrics
interpretation, final analysis ZIP and final acceptance backup.

Gaps: unverified standalone P2 checkpoint; optional usable CTC head/alignment;
Pi/ARM64/2-GB system performance; contextual reconstruction punctuation gold;
per-process native WDDM VRAM; automatic guarded LLM continuation.

Keep A0 and compact A1 represented regardless of larger-model desktop WER.
No winner is selected from the small screen. Do not start N4 here.
