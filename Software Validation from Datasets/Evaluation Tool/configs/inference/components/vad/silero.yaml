component:
  slot: vad
  name: silero_vad
  enabled: true
  adapter: SileroVADAdapter
  notes: Lazy Silero adapter config; requires local model assets before use.
  params:
    model_family: silero
    threshold: 0.5
    min_speech_ms: 250
    min_silence_ms: 100
    pad_ms: 30
    sample_rate: 16000
