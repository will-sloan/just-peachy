component:
  slot: diarization
  name: pyannote_community
  enabled: true
  adapter: PyannoteCommunityDiarizer
  notes: Optional pyannote community diarization helper; disabled by default because model access may be gated.
  params:
    model_source: models/cache/pyannote/speaker-diarization-community-1
    min_turn_sec: 0.05
    min_speakers: null
    max_speakers: null
    collar_sec: 0.0
    auth_token_env: HF_TOKEN
    cache_dir: models/cache/pyannote
    allow_model_downloads: false
