{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "extended_screening_smoke.v1.schema.json",
  "type": "object",
  "required": ["schema_version", "scope", "scientific_screening_complete", "comparison_item", "summary", "results", "secret_audit"],
  "properties": {
    "schema_version": {"const": "extended-screening-smoke.v1"},
    "scientific_screening_complete": {"const": false},
    "results": {"type": "array", "minItems": 1},
    "secret_audit": {
      "type": "object",
      "properties": {"values_serialized": {"const": false}}
    }
  }
}
