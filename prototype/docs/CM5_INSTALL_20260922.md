# CM5 installation started 2026-09-22

**Current update:** rc4 is installed and running. See
[CM5_MOTION_3D_20260922.md](CM5_MOTION_3D_20260922.md). The sensor is electrically
connected but not mounted yet; motion compensation is inactive until the
one-time assembly confirmation. The rc3 details below are historical.

Purpose: bring the current Windows/CM5 prototype onto the user's assembled CM5,
using existing models and recipes. No training, research sweep or private
Windows profile/session transfer is part of this installation. The later
authorized no-USB bring-up includes the firmware change recorded below.

## Current verified status (supersedes initial pending notes below)

- Correct DSI display cable resolved the dark Freenove screen. CAM/DISP0 uses
  `vc4-kms-dsi-7inch,dsi0`; portrait transform 90 is persisted in kanshi.
  User confirms both orientation and touch. Existing touch mapping is preserved.
- XTAG4 `6H5HF67B` detected the single XS3A target. xflash completed successfully
  for XMOS 3.2.1 `intdev-lr48-lin-i2c` (onboard linear array, internal clock
  recovery). Readback on Pi: VERSION 3 2 1 and matching BLD_MSG.
- Native aarch64 host tool, USB/I2C libraries and command map built successfully.
  Vendor XMOS I2S overlay and IO-expander setup installed; boot service active.
  Device is `XMOSDevice: 1f000a0000.i2s-dir-hifi dir-hifi-1 (hw:2,1)`.
- Real 10-second capture via I2S + I2C: 160480 model samples, RMS 0.0008261,
  peak 0.0065068; integrity and route restoration passed. Beam telemetry RUNNING.
  No WAV saved. Ambient signal does not prove speech/identity accuracy.
- BMI270 detected at I2C1 0x68, chip ID 0x24. User confirms fixed common chassis.
  Bosch SensorAPI native build passed. Initial 8-second test: 301 samples,
  stationary/valid, no error, 0.0233 CPU seconds (~0.3% of one core).
- Microphone permission is saved for this user. The current source removes
  repeated prompts and supports one-shot listening when opened; Stop remains.
  Startup activation and final offline/native results are recorded in the
  deployment receipt rather than inferred from software tests.

Reproduction, firmware hashes, wiring, rollback and commands:
[CM5_I2S_MIGRATION.md](CM5_I2S_MIGRATION.md).
Motion behavior, front/back assumptions and limitations:
[README_IMU.md](../app/README_IMU.md). Physical known-angle rotation accuracy
has not yet been validated; relative assistance is experimental.

Local evidence: `G:\Just_Peachy_PROTO1\cm5_bringup_20260922\native_evidence`.
Target evidence: `~/JustPeachy/checks/20260922`.

## Earlier chronological installation record

## Verified target

- Compute Module 5 Rev 1.0, aarch64, 2 GB RAM, 32 GB eMMC, no wireless.
- Debian/Raspberry Pi OS Bookworm, Python 3.11.2; initial 22 GB free on root.
- Wired address `192.168.2.57`, actual hostname `raspberrypi`.
- Account `peachyprototype`; password entered privately by the user.
- Dedicated Windows key: `%USERPROFILE%\.ssh\just_peachy_cm5_ed25519`.
- Initial temperature 39.5 C, throttle flags 0x0; not a sustained-load test.
- The initial native inspection showed no DSI connector and no XVF USB device.
  Display wiring/controller and real live operation remain to be verified.

OS prerequisites were installed from the configured Bookworm repositories:
python3.11-venv, python3-tk, libportaudio2, libsndfile1, libusb-1.0-0, libgomp1,
cmake, build-essential, pkg-config, libusb-1.0-0-dev, python3-yaml, python3-jinja2,
alsa-utils, file and binutils. Eight missing packages were added; no distribution
upgrade, firmware update or new sudo rule was performed.

## Source export

Candidate version: `proto1-cm5-20260922-rc1`. Source staging and receipts live in
`G:\Just_Peachy_PROTO1\cm5_bringup_20260922`; immutable archives live in
`G:\Just_Peachy_PROTO1\releases`.

The source is the current prototype, including tasks01-12 and the closed-group
display fix. `MODE_GUIDE.md` remains open in Word on Windows. The release staging
copy substitutes the already-verified `MODE_GUIDE_UPDATED.md` under the canonical
name; it does not close Word or overwrite any unsaved document. Other allowed
payload files are copied unchanged. Private data, research, tests and evidence
are excluded using the existing release payload policy. The previous task08 ZIP
remains immutable and is not presented as current.

Release-tool tests: 22 passed locally before this export. Previous exact app
behavior checks: 329 software checks plus two bounded native Windows cases;
those are not a claim of native CM5 qualification.

## Transfer and launch commands

Inputs: candidate ZIP, existing ARM64 CPython 3.11 wheelhouse, shared models,
verified SSH target/key. Outputs: isolated source/runtime release, shared model
store, and fresh external Pi data directory. No production Windows profiles
are copied. Use the existing installer; it verifies archive/model hashes.

PowerShell, repository root:

```powershell
& .\prototype\release_tools\deploy_pi.ps1 -PiHost '192.168.2.57' -UserName 'peachyprototype' -IdentityFile "$env:USERPROFILE\.ssh\just_peachy_cm5_ed25519" -Archive 'G:\Just_Peachy_PROTO1\releases\just-peachy-proto1-cm5-20260922-rc1.zip' -RemoteRoot '/home/peachyprototype/JustPeachy/install' -RemoteDataRoot '/home/peachyprototype/JustPeachy/data' -Wheelhouse 'G:\Just_Peachy_PROTO1\arm64 cp311 wheels' -Models 'C:\Users\amiri\JustPeachy\shared\models' -Activate -DryRun
```

Review the dry run; remove `-DryRun` to install. In CMD/Anaconda Prompt invoke
the same script with `powershell.exe -NoProfile -ExecutionPolicy Bypass -File`,
use double quotes for paths, and replace `$env:USERPROFILE` with `%USERPROFILE%`.
SSH's strict host-key checking remains enabled. No Anaconda activation is needed.

From a terminal on the Pi's graphical desktop, after activation:

```bash
python3.11 "$HOME/JustPeachy/install/releases/proto1-cm5-20260922-rc1/release_tools/launch_current.py" --root "$HOME/JustPeachy/install" --data-root "$HOME/JustPeachy/data"
```

The GUI starts idle. Live input requires a qualified native ARM64 XVF helper,
actual USB endpoint and external live_config.json. No Windows executable or
endpoint GUID is suitable for the Pi. Optional expensive features stay at their
existing defaults. Check file inference, touchscreen orientation, Stop/Start,
audio routing and whole-system resources before extended real-person use.

## Executed checks, 2026-09-22

Installation and activation completed on the actual CM5. The installer verified
all 216 payload files and all eight model files, installed the 13 pinned ARM64
dependencies, and created a fresh external data root. Archive SHA256:
`c8f787b1707200634c269047a5785254b662116c6dc401b2247085d930b03f79`.

A bounded native saved-file test passed in Captions / fast / O0 mode. Input was
the existing 12-second public CMU ARCTIC fixture `A_12s.wav`, SHA256
`59ffff183856bb4072a21291677537c28833e3a60945ad8a0983756c1cdf9861`.
It produced a nonempty final transcript, returned STOPPED with no error, exited
with code 0 and left no data-root lock. Total process time including model load
was 15.89 seconds; maximum child-process RSS was 413152 KiB. This is a startup
smoke check, not a sustained real-time performance or speaker-mode qualification.
Post-test temperature was 41.7 C and throttle flags were 0x0. No microphone was
opened. The first launch wrapper did not start the application because the OS
does not include `/usr/bin/time`; the successful run used Python's standard
`time` and `resource` modules, without adding an OS dependency.

Evidence: `/home/peachyprototype/JustPeachy/checks/20260922/replay-result.json`,
`replay-time.json`, and `replay.log`; copies are in
`G:\Just_Peachy_PROTO1\cm5_bringup_20260922\native_evidence`.
To repeat on the Pi, use a new data-root/result path so existing evidence remains:

```bash
JP_BASE="$HOME/JustPeachy"
"$JP_BASE/install/runtimes/proto1-cm5-20260922-rc1/bin/python" "$JP_BASE/install/releases/proto1-cm5-20260922-rc1/main.py" file --wav "$JP_BASE/checks/20260922/A_12s.wav" --models "$JP_BASE/install/models" --data-root "$JP_BASE/checks/replay-repeat-data" --mode caption_only --recipe fast --tap O0 --result "$JP_BASE/checks/replay-repeat.json"
```

From PowerShell or CMD/Anaconda Prompt, open the SSH terminal first (no Conda
environment activation is required), then use the Linux command above:

```powershell
ssh.exe -i "$env:USERPROFILE\.ssh\just_peachy_cm5_ed25519" -o StrictHostKeyChecking=yes peachyprototype@192.168.2.57
```

In CMD/Anaconda Prompt, replace `$env:USERPROFILE` with `%USERPROFILE%`.

## Remaining physical/native checks

The display remains blank and the current boot configuration has no DSI panel
overlay. No DSI connector appears in DRM. Compute Modules do not automatically
detect DSI displays. Do not choose a panel overlay until its controller/revision
is identified, and do not apply the unrelated Touch Display 2 overlay.

Freenove's FNK0078 H and A/B/C assembly diagrams show spring contacts touching
the underside of an ordinary Pi's GPIO header. The simplified "mount and attach
ribbon" instructions therefore do not establish that a ribbon alone powers this
screen on CM5IO. The exact screen rear/connector labels and power arrangement
remain unverified. Inspect photos of the assembly before giving power pinouts;
reseat ribbons only after a clean shutdown and disconnection from power.

Latest USB inventory contains root hubs only: XVF USB is not yet enumerated on
the CM5. Native ARM64 XVF host/map qualification, live USB routing, graphical
display/touch, enrollment/spatial live checks and combined sustained resource
measurement remain pending. The release archive remains immutable; these newer
installation observations are external evidence, not changed archive claims.

Sources: [Freenove manuals](https://github.com/Freenove/Freenove_Touchscreen_Monitor_for_Raspberry_Pi)
and [Raspberry Pi display/Compute Module instructions](https://www.raspberrypi.com/documentation/accessories/display.html).

## Display bring-up follow-up

User photo shows a blue `7inch DSI Display`, `800X480 Pixel`, `Rev0.1` board
with two spring power contacts and a separate two-pin socket labelled GND/+5V
beside the host ribbon connector. The white control labelled Backlight is a
button, as confirmed by the manufacturer's FAQ; it is not the power socket.
The user reports attaching the supplied power cable through the XVF 40-pin
pass-through. The exact header orientation/power continuity has not been
independently photographed or measured; do not represent it as verified.

After this wiring change the CM5 booted and SSH succeeded. USB still enumerated
root hubs only. `get_throttled=0x50000` recorded undervoltage and throttling
earlier in that boot, with neither condition active at the observation. Supply
and combined peripheral load still need verification before sustained testing.

The panel configuration test saves the original boot file at
`/boot/firmware/config.txt.pre-peachy-dsi-20260922`, disables display autodetection,
and adds `dtoverlay=vc4-kms-dsi-7inch,dsi0` under `[all]`, retaining KMS. This
tests the legacy 800x480 display family on the user-reported CAM/DISP0 port;
successful driver probe and actual visible picture/touch remain separate checks.
Change hashes are in `checks/20260922/display-config.json` on the Pi.

Rollback, from an SSH terminal after stopping the application:

```bash
sudo cp /boot/firmware/config.txt.pre-peachy-dsi-20260922 /boot/firmware/config.txt
sudo sync
sudo reboot
```

Use the SSH command above from PowerShell or CMD/Anaconda Prompt. Do not apply
legacy FKMS instructions or a 720x1280 Touch Display 2 overlay to this panel.

Post-overlay reboot: SSH recovered at the same address. DSI driver/framebuffer
now registers `card1-DSI-1`, enabled at 800x480, and backlight node `10-0045`
exists. These are configured driver objects, NOT proof that the panel works.
Kernel I2C controller `1f00088000.i2c` repeatedly times out; panel backlight
enable failed with -110 and `edt_ft5x06 10-0038` touchscreen probe failed with
-110. No touchscreen input device registered. Evidence is in
`display-after-reboot.json` on the Pi and the Windows native_evidence folder.
Latest throttle flags after this reboot were 0x0. Next check is a photograph
of the actual assembled power lead/header orientation and CAM/DISP0 ribbon
connection, then (if needed) direct power/continuity verification. Driver
compatibility is not conclusively established by the failed probe. No pin
rewiring, firmware changes or alternate display overlays were tried remotely.

### XVF connector identified from user photos

Later photographs identify the exact microphone board as XMOS XK-VOICE-SQ66,
PCB JGB0084 1V1. The matching local vendor design archive is
`C:\Users\amiri\Downloads\XK-VOICE-SQ66-Design-Files_1V1.zip`.
Schematic sheet 4 (Host) confirms J4 pins 2/4 are PI_5V and pins
6/9/14/20/25/30/34/39 are GND. Assembly sheet 1 shows pin 1 at the lower-left
of J4 in the front/component view (microphone bar at top, board cutout at left).
Thus in the user's rear-view Photo 1 (microphone bar at top, cutout at right),
pin 1 is lower-right, pin 2 upper-right, and numbering increases leftwards by
two in each row. Upper-row third socket from the right is pin 6 (GND);
lower-row fifth socket from the right is pin 9 (GND). Upper-row first/second
sockets from the right are pins 2/4 (5V). The individual plugged holes and
end-to-end ribbon continuity are NOT conclusively visible in these photos.
Do not mark the existing display wiring as passed from these images alone.
The schematic and assembly PDFs are retained in the bring-up vendor_docs/SQ66_1V1
directory. No user connection was changed during this visual review.

The user subsequently measured positive 5 V at the display power input with a
multimeter. This supports correct voltage/polarity at those measurement points;
it does not establish all 40 ribbon conductors, DSI communication, or behaviour
under load. Retain the power wiring while inspecting the separate DSI path.
At the immediate follow-up the SSH connection to 192.168.2.57 timed out, so the
earlier panel/touch I2C errors are the last verified remote display evidence,
not a fresh observation after the user's voltage measurement.

Next user-requested connection succeeded at 192.168.2.57 after a new boot
(remote uptime reported 0 minutes at 17:04 in the Pi's local time). OS hostname remains raspberrypi;
labwc desktop session is running. DSI framebuffer remains enabled at 800x480.
Backlight enable and edt_ft5x06 touchscreen probe now fail with -121 (remote
I/O/no acknowledgement) instead of the earlier -110 controller timeouts; no
touchscreen input is registered. This change does not establish correct DSI
wiring or panel compatibility. Startup throttle history is again 0x50000;
kernel voltage-normalised messages appear at approximately 9, 13, 19 and 47
seconds. No active flag was set at the initial SSH observation. The exact
official USB-C PSU output rating has been requested before further changes.
USB inventory still contains root hubs only; XVF USB audio is not enumerated.

The user confirmed the official 27 W supply, labelled 5.1 V / 5.0 A. A subsequent
fresh-boot snapshot at 17:07 in the Pi's local time showed throttle flags 0x0, no voltage events in
that boot's kernel log, and PMIC EXT5V_V = 5.1054 V. Do not misreport the previous
boot's warnings as currently active or recommend replacement solely from the
PSU rating. Snapshot saved on Pi as `checks/20260922/power-followup.json`.

### After CAM/DISP0 ribbon reseating

The user's close-up confirms the physical port is J5 / CAM/DISP0. Raspberry Pi's
CM IO board instructions require the exposed ribbon contacts to face down,
toward the PCB. The corresponding Freenove A/B/C manual, page 2, shows the
display-end contacts facing up, away from the blue display PCB. Copper traces
visible through amber insulation alone do not identify exposed contact faces.
Do not blindly flip both ends; inspect the actual exposed fingers with all power
disconnected, seat squarely and fully, then secure the retaining clips.

After a remote clean shutdown and the user's reported cable correction/reboot,
SSH recovered. At 2026-09-22T16:19:52Z the boot ID was
`e03e8781-67a0-4251-96e6-685e6b975a61`, throttle flags were 0x0, and the user
reported the screen still completely dark. Backlight enable and touch probe
both fail with -110 (I2C timeout); no touchscreen input device is registered.
A read-only pinctrl snapshot shows CAM/DISP0 SDA0/GPIO38 high and SCL0/GPIO39
low; a later snapshot agrees. This narrows investigation toward a stalled
control bus, but does not distinguish cabling, panel electronics, or driver
compatibility on its own. No GPIO levels were forced and no bus scan was run.
Evidence: Pi `checks/20260922/display-after-ribbon-reseat.json`.

The manufacturer documents a brief Backlight button press to adjust/restore
brightness and a long press to turn it off. The user performed the short-press
check and reported the screen remained completely dark. A remote clean shutdown
was issued successfully before requesting inspection/reseating of the
display-end ribbon; that inspection is pending. Leave the current KMS
configuration unchanged during these checks.
Do not apply the FAQ's older FKMS/legacy graphics instructions to CM5.

Sources:
- https://www.raspberrypi.com/documentation/accessories/touch-display-2.html
  (CM IO board connector direction only, not that panel's overlay)
- https://github.com/Freenove/Freenove_Touchscreen_Monitor_for_Raspberry_Pi/blob/main/Tutorial_FNK0078_A_B_C.pdf
- https://docs.freenove.com/projects/fnk0078/en/latest/fnk0078/codes/tutorial/FAQS.html

The user subsequently confirmed exposed contacts face up at the screen end.
After the next power-on, an initial IPv4 SSH attempt timed out, but
`raspberrypi.local` resolved and SSH succeeded with `HostKeyAlias=192.168.2.57`
and strict host-key verification against the existing trusted key. The Pi
still reports IPv4 192.168.2.57; do not claim a DHCP address change. Boot ID
`6d4c7117-d9dd-428e-9c91-6578e6cb5087` again has throttle flags 0x0, SCL0 low,
SDA0 high, backlight/touch -110 failures and no touchscreen input device.
Receipt: Pi `checks/20260922/display-both-ends-checked.txt`.

A clean shutdown was then sent before requesting one isolation test: with all
power disconnected, unplug the signal ribbon from CAM/DISP0, leaving the power
wiring unchanged, and reboot with that ribbon end safely clear of electronics.
This test intentionally cannot show a screen image; its purpose is to check
whether the control-bus clock returns high with the cable/display detached.
This isolation result is pending. No further boot-config changes were made.

Isolation result at 2026-09-22T16:26:05Z: after the user powered on following the
requested CAM/DISP0 disconnection, SSH succeeded, boot ID
`c072a26a-9c8b-441b-9cc0-dd9296540887`. Both SDA0/GPIO38 and SCL0/GPIO39 now read
high; throttle flags remain 0x0. The -110 controller timeouts disappeared.
Backlight/touch probes instead return -121, expected with the panel absent.
This implicates the attached cable/display path in the prior low-clock state;
it does not yet prove a defective panel, defective cable, or complete CM5
display-interface functionality. Receipt: `display-ribbon-disconnected.txt`.

The Pi was cleanly shut down again before requesting the next isolation step:
with all power removed, disconnect this cable at the screen, reconnect its CM5
end to CAM/DISP0 in the documented orientation, secure the loose screen end
away from exposed circuitry, and boot with the cable attached only to the CM5.
Keep other wiring unchanged. The cable-only result is pending.

Cable-only result at 2026-09-22T16:28:08Z: SSH succeeded, boot ID
`06d4b14c-76ce-41b1-a30a-7bee41d95075`; SDA0/SCL0 both high, throttle 0x0,
no controller timeouts, expected absent-panel backlight/touch -121 errors.
This rules out an always-present clock short with the cable alone in that
position, but does not validate its end-to-end mapping, contacts or DSI data.

The user then identified the installed ribbon as a Raspberry Pi CAMERA cable
and reported having a DISPLAY cable to substitute. Treat incorrect cable type
as the leading explanation, not a confirmed repair until the replacement
produces successful display/touch operation. A proposed temporary driver-off,
I2C-only boot diagnostic was NOT applied: its SSH connection timed out before
any remote code executed. Thus no diagnostic config backup or driver-disable
change was created by that attempt. A further read-only IPv4 connection also
timed out; no new shutdown command succeeded during this turn. Last verified
boot config remains the normal `vc4-kms-dsi-7inch,dsi0` setup above.
The user was instructed to remove power before replacing the cable.

Official display cable product reference:
https://www.raspberrypi.com/products/display-cable/
